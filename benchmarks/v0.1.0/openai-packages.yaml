# toolcalls.yaml
# What your agent can do to the real world — and what checks exist.
# Generated by diplomat-agent-ts · https://diplomat.run
#
# Commit this file. Review changes in PRs.
# In source code:
#   Fix          → add validation, the next scan picks it up
#   Acknowledge  → add  # checked:ok  to confirm you've reviewed it
#   Protected elsewhere → add  # checked:ok — protected by [middleware/gateway/etc]
#   CI enforcement → --fail-on-unchecked blocks PRs with new unreviewed tool calls

generated: "2026-05-15T14:28:41.333Z"
version: "0.1.0"
spec_version: "1.0"
path: "/tmp/diplomat-benchmark/openai-agents-js/packages"
language: typescript

summary:
  total: 33
  no_checks: 31
  partial_checks: 2
  confirmed: 0

tool_calls:

  # ⚠ NO CHECKS — dynamic_code / file_delete
  - function: adaptBlaxelSandbox
    file: agents-extensions/src/sandbox/blaxel/sandbox.ts
    line: 1297
    actions:
      - "await sandbox.fs.rm(path, recursive);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: deleteRemotePath
    file: agents-extensions/src/sandbox/blaxel/sandbox.ts
    line: 747
    actions:
      - await this.sandbox.fs.rm(path);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: getExposedGc
    file: agents-core/test/manual/gcRuntime.ts
    line: 6
    actions:
      - "const exposedGc = vm.runInNewContext('gc');"
    checks: []
    missing:
      - no input validation on dynamic code
    owasp: [ASI-02, ASI-03]

  - function: writeMountSecretFile
    file: agents-extensions/src/sandbox/blaxel/sandbox.ts
    line: 604
    actions:
      - "await this.sandbox.fs.rm(path).catch(() => {});"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: writeRemoteFile
    file: agents-extensions/src/sandbox/blaxel/sandbox.ts
    line: 677
    actions:
      - "await this.sandbox.fs.rm(tempPath).catch(() => {});"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: writeRemoteTempFile
    file: agents-extensions/src/sandbox/blaxel/sandbox.ts
    line: 725
    actions:
      - "await this.sandbox.fs.rm(tempPath).catch(() => {});"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]


  # ⚠ NO CHECKS — agent_invocation
  - function: asTool
    file: agents-core/src/agent.ts
    line: 677
    actions:
      - "? await runner.run(this, runInput, {"
      - ": await runner.run(this, runInput, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: run
    file: agents-core/src/run.ts
    line: 397
    actions:
      - "return await runner.run(agent, input, options);"
      - "return await runner.run(agent, input, options);"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]


  # ⚠ NO CHECKS — destructive / http_write
  - function: #clearAcceptedResponseCreate
    file: agents-realtime/src/responseCreateSequencer.ts
    line: 279
    actions:
      - "this.#pendingRequestVersions.delete(version);"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: #clearPendingResponseCreate
    file: agents-realtime/src/responseCreateSequencer.ts
    line: 250
    actions:
      - "this.#pendingRequestVersions.delete("
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: assertPtyBridgePythonAvailable
    file: agents-core/src/sandbox/sandboxes/shared/pty.ts
    line: 150
    actions:
      - "const result = spawnSync("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: close
    file: agents-extensions/src/sandbox/cloudflare/sandbox.ts
    line: 620
    actions:
      - "const response = await this.fetch("
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: connect
    file: agents-realtime/src/openaiRealtimeWebRtc.ts
    line: 154
    actions:
      - "const sdpResponse = await fetch(connectionUrl, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: create
    file: agents-extensions/src/sandbox/cloudflare/sandbox.ts
    line: 1026
    actions:
      - await fetchWithOptionalTimeout(
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: createSandboxHttpEventSink
    file: agents-core/src/sandbox/events.ts
    line: 88
    actions:
      - "const response = await fetch(options.endpoint, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: execShell
    file: agents-extensions/src/sandbox/cloudflare/sandbox.ts
    line: 711
    actions:
      - "const response = await this.fetch("
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: hydrateWorkspace
    file: agents-extensions/src/sandbox/cloudflare/sandbox.ts
    line: 585
    actions:
      - "const response = await this.fetch("
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: mountBucket
    file: agents-extensions/src/sandbox/cloudflare/sandbox.ts
    line: 496
    actions:
      - "const response = await this.fetch("
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: persistWorkspace
    file: agents-extensions/src/sandbox/cloudflare/sandbox.ts
    line: 553
    actions:
      - "const response = await this.fetch("
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: runDockerProcess
    file: agents-core/src/sandbox/sandboxes/docker.ts
    line: 1185
    actions:
      - "const child = spawn('docker', args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runSandboxProcess
    file: agents-extensions/src/sandbox/shared/process.ts
    line: 22
    actions:
      - "const child = spawn(command, args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runSandboxProcess
    file: agents-core/src/sandbox/sandboxes/shared/runProcess.ts
    line: 22
    actions:
      - "const child = spawn(command, args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: spawnInPseudoTerminal
    file: agents-core/src/sandbox/sandboxes/shared/pty.ts
    line: 120
    actions:
      - return spawn(
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: spawnShellCommand
    file: agents-core/src/sandbox/sandboxes/docker.ts
    line: 421
    actions:
      - "return spawn('docker', dockerArgs, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: spawnShellCommand
    file: agents-core/src/sandbox/sandboxes/unixLocal.ts
    line: 629
    actions:
      - "return spawn(shellPath, [flag, command], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: unmountBucket
    file: agents-extensions/src/sandbox/cloudflare/sandbox.ts
    line: 528
    actions:
      - "const response = await this.fetch("
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: writeFile
    file: agents-extensions/src/sandbox/cloudflare/sandbox.ts
    line: 901
    actions:
      - "const response = await this.fetch("
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]


  # ⚠ NO CHECKS — publish
  - function: adaptDaytonaSandbox
    file: agents-extensions/src/sandbox/daytona/sandbox.ts
    line: 1331
    actions:
      - "await sandbox.fs.uploadFile(source, remotePath, timeout);"
      - "await sandbox.fs.uploadFile(source, remotePath);"
      - "await sandbox.fs.uploadFile(source, remotePath, timeout);"
      - "await sandbox.fs.uploadFile(source, remotePath);"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: archiveIo
    file: agents-extensions/src/sandbox/daytona/sandbox.ts
    line: 802
    actions:
      - "await this.sandbox.fs.uploadFile(Buffer.from(content), path);"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: materializeMountEntry
    file: agents-extensions/src/sandbox/daytona/sandbox.ts
    line: 709
    actions:
      - "await this.sandbox.fs.uploadFile(Buffer.from(content), path);"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: writer
    file: agents-extensions/src/sandbox/daytona/sandbox.ts
    line: 683
    actions:
      - "await this.sandbox.fs.uploadFile(Buffer.from(content), path);"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]


  # ~ PARTIAL CHECKS
  - function: #fetchResponse
    file: agents-openai/src/openaiChatCompletionsModel.ts
    line: 375
    actions:
      - "const completion = await this.#client.chat.completions.create("
    checks:
      - type: input_validation
        code: if (request.systemInstructions)
    missing:
      - no rate limit
      - no retry bound
    owasp: [ASI-02, ASI-05, ASI-06]

  - function: export
    file: agents-openai/src/openaiTracingExporter.ts
    line: 720
    actions:
      - "const response = await fetch(this.#options.endpoint, {"
    checks:
      - type: input_validation
        code: "if (attempts >= this.#options.maxRetries)"
      - type: input_validation
        code: "if (attempts >= this.#options.maxRetries)"
    missing:
      - no rate limit
    owasp: [ASI-02, ASI-06]

