# toolcalls.yaml
# What your agent can do to the real world — and what checks exist.
# Generated by diplomat-agent-ts · https://diplomat.run
#
# Commit this file. Review changes in PRs.
# In source code:
#   Fix          → add validation, the next scan picks it up
#   Acknowledge  → add  # checked:ok  to confirm you've reviewed it
#   Protected elsewhere → add  # checked:ok — protected by [middleware/gateway/etc]
#   CI enforcement → --fail-on-unchecked blocks PRs with new unreviewed tool calls

generated: "2026-05-15T11:55:25.704Z"
version: "0.1.0"
spec_version: "1.0"
path: "/tmp/diplomat-benchmark/mastra/packages"
language: typescript

summary:
  total: 185
  no_checks: 162
  partial_checks: 23
  confirmed: 0

tool_calls:

  # ⚠ NO CHECKS — database_delete / file_delete
  - function: atomicWriteFile
    file: core/src/llm/model/registry-generator.ts
    line: 21
    actions:
      - await fs.unlink(tempPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: atomicWriteFileSync
    file: core/src/llm/model/provider-registry.ts
    line: 85
    actions:
      - fs.unlinkSync(tempPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cloneRepositoryWithoutGit
    file: cli/src/utils/clone-template.ts
    line: 82
    actions:
      - "await exec(degitCommand, {"
      - "await exec(gitCommand, {"
      - "await fs.rm(gitDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no input validation
      - no auth check
      - no confirmation step
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: copyLlmsTxtFiles
    file: mcp-docs-server/scripts/prepare-docs.ts
    line: 73
    actions:
      - "await fs.rm(DOCS_DEST, { recursive: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: deleteFile
    file: core/src/workspace/filesystem/local-filesystem.ts
    line: 481
    actions:
      - await fs.unlink(absolutePath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: deleteLLMRecording
    file: _llm-recorder/src/llm-recorder.ts
    line: 1272
    actions:
      - fs.unlinkSync(recordingPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: destroy
    file: core/src/workspace/sandbox/local-sandbox.ts
    line: 284
    actions:
      - await fs.unlink(this._seatbeltProfilePath);
      - await fs.rmdir(this._sandboxFolderPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: getPgStorageTests
    file: memory/integration-tests/src/shared/with-pg-storage.ts
    line: 155
    actions:
      - "await client.query(`"
      - "const stream = await agent.stream("
    checks: []
    missing:
      - no confirmation step
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-03, ASI-04, ASI-06, ASI-10]

  - function: loadRegistry
    file: core/src/llm/model/provider-registry.ts
    line: 233
    actions:
      - fs.unlinkSync(jsonPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: mount
    file: core/src/workspace/sandbox/local-sandbox.ts
    line: 400
    actions:
      - await fs.rmdir(hostPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: moveFile
    file: core/src/workspace/filesystem/local-filesystem.ts
    line: 575
    actions:
      - "await fs.rm(srcPath, { recursive: true, force: true });"
      - "await fs.rm(srcPath, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: rmdir
    file: core/src/workspace/filesystem/composite-filesystem.ts
    line: 384
    actions:
      - "return r.fs.rmdir(r.fsPath, options);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: rmdir
    file: core/src/workspace/filesystem/local-filesystem.ts
    line: 640
    actions:
      - "await fs.rm(absolutePath, { recursive: true, force: options?.force ?? false });"
      - await fs.rmdir(absolutePath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: syncGlobalCacheToLocal
    file: core/src/llm/model/provider-registry.ts
    line: 108
    actions:
      - fs.unlinkSync(GLOBAL_PROVIDER_REGISTRY_JSON());
      - fs.unlinkSync(GLOBAL_PROVIDER_TYPES_DTS());
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: unmount
    file: core/src/workspace/sandbox/local-sandbox.ts
    line: 509
    actions:
      - await fs.unlink(markerPath);
      - await fs.unlink(hostPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]


  # ⚠ NO CHECKS — agent_invocation
  - function: #drainPendingSignals
    file: core/src/agent/thread-stream-runtime.ts
    line: 207
    actions:
      - "const output = await previousRun.agent.stream(signal, {"
      - "const output = await pendingIdle.agent.stream(pendingIdle.signal, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: agentGenerate
    file: _test-utils/src/llm-helpers.ts
    line: 121
    actions:
      - "return agent.generate(message, transformedOptions as any);"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: agentStream
    file: _test-utils/src/llm-helpers.ts
    line: 164
    actions:
      - "return agent.stream(message, transformedOptions as any);"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: call
    file: memory/src/processors/observational-memory/observer-runner.ts
    line: 112
    actions:
      - "const streamResult = await agent.stream(observerMessages, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: callMultiThread
    file: memory/src/processors/observational-memory/observer-runner.ts
    line: 237
    actions:
      - "const streamResult = await agent.stream(observerMessages, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: convertAgentsToTools
    file: mcp/src/server/server.ts
    line: 1022
    actions:
      - "const response = await agent.generate(message, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: createSubagentTool
    file: core/src/harness/tools.ts
    line: 822
    actions:
      - "const response = await subagentToRun.stream(task, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: detectLanguage
    file: core/src/processors/processors/language-detector.ts
    line: 287
    actions:
      - "const response = await this.detectionAgent.generate(prompt, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: detectPII
    file: core/src/processors/processors/pii-detector.ts
    line: 277
    actions:
      - "const response = await this.detectionAgent.generate(prompt, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: detectPromptInjection
    file: core/src/processors/processors/prompt-injection-detector.ts
    line: 213
    actions:
      - "const response = await this.detectionAgent.generate(prompt, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: detectSystemPrompts
    file: core/src/processors/processors/system-prompt-scrubber.ts
    line: 263
    actions:
      - "const response = await this.detectionAgent.generate(text, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: doGenerate
    file: memory/src/processors/observational-memory/observer-runner.ts
    line: 148
    actions:
      - "const streamResult = await agent.stream(observerMessages, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: doGenerate
    file: memory/src/processors/observational-memory/observer-runner.ts
    line: 281
    actions:
      - "const streamResult = await agent.stream(observerMessages, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: executeAgent
    file: core/src/datasets/experiment/executor.ts
    line: 198
    actions:
      - "? await agent.generate(input, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: executeAgent
    file: core/src/evals/run/index.ts
    line: 333
    actions:
      - "? await agent.generate(item.input, { ...baseOptions, structuredOutput })"
      - ": await agent.generate(item.input, baseOptions);"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: executeGenerate
    file: server/src/server/handlers/responses.ts
    line: 333
    actions:
      - "return (await agent.generate(input, commonOptions as never)) as ResponseExecutionResult;"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: executeStream
    file: server/src/server/handlers/responses.ts
    line: 399
    actions:
      - "return (await agent.stream(input, commonOptions as never)) as ResponseStreamResult;"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: extract
    file: rag/src/document/extractors/schema.ts
    line: 25
    actions:
      - "const result = await agent.generate([{ role: 'user', content: node.getContent() }], {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: extractKeywordsFromNodes
    file: rag/src/document/extractors/keywords.ts
    line: 57
    actions:
      - "const result = await miniAgent.generate(["
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: extractQuestionsFromNode
    file: rag/src/document/extractors/questions.ts
    line: 57
    actions:
      - "const result = await miniAgent.generate([{ role: 'user', content: prompt }]);"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: extractTitles
    file: rag/src/document/extractors/title.ts
    line: 108
    actions:
      - "const result = await miniAgent.generate(["
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: generateFinalResult
    file: core/src/loop/network/validation.ts
    line: 542
    actions:
      - "const stream = await agent.stream(prompt, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: generateNodeSummary
    file: rag/src/document/extractors/summary.ts
    line: 58
    actions:
      - "const result = await miniAgent.generate([{ role: 'user', content: prompt }]);"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: generateStructuredFinalResult
    file: core/src/loop/network/validation.ts
    line: 634
    actions:
      - "const stream = await agent.stream<OUTPUT>(prompt, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: getInputProcessorsTests
    file: memory/integration-tests/src/shared/input-processors.ts
    line: 27
    actions:
      - "await agent.generate('My name is Alice', {"
      - "await agent.generate('I live in Paris', {"
      - "const thirdResponse = await agent.generate('What is my name and where do I live?', {"
      - "await agent.generate('Message 1', { memory: { thread: threadId, resource: resourceId }, maxSteps: 1 });"
      - "await agent.generate('Message 2', { memory: { thread: threadId, resource: resourceId }, maxSteps: 1 });"
      - "await agent.generate('Message 3', { memory: { thread: threadId, resource: resourceId }, maxSteps: 1 });"
      - "const fourthResponse = await agent.generate('Message 4', {"
      - "const response = await agent.generate('What is my occupation?', {"
      - "const response = await agent.generate('What are my preferences?', {"
      - "await agent.generate('I love programming in Python, especially for data science', {"
      - "await agent.generate('Python has great libraries like pandas and numpy', {"
      - "const response = await agent.generate('What programming languages have we discussed?', {"
      - "await agent.generate('I like cats', { memory: { thread: thread1Id, resource: resourceId } });"
      - "await agent.generate('I like dogs', { memory: { thread: thread1Id, resource: resourceId } });"
      - "await agent.generate('I like birds', { memory: { thread: thread1Id, resource: resourceId } });"
      - "const response = await agent.generate('Tell me about cats', {"
      - "await agent.generate('I really love apples, they are my favorite fruit', {"
      - "await agent.generate('Cats are wonderful pets and companions', {"
      - "await agent.generate('Programming in JavaScript is fun', {"
      - "await agent.generate('Mountains are great for hiking and skiing', {"
      - "const response = await agent.generate('What do you know about apples?', {"
      - "await agent.generate('I work with React', { memory: { thread: thread1Id, resource: resourceId } });"
      - "await agent.generate('Hello', { memory: { thread: thread2Id, resource: resourceId } });"
      - "const response = await agent.generate('What do I work with?', {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: getMessageOrderingTests
    file: memory/integration-tests/src/shared/message-ordering.ts
    line: 188
    actions:
      - "const stream = await agent.stream(\"What's the weather in San Francisco?\", {"
      - "const stream = await agent.stream('Research weather patterns in CA and create a summary.', {"
      - "const stream = await agent.stream(\"What's the weather in NYC?\", {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: getOutputProcessorMemoryTests
    file: memory/integration-tests/src/shared/output-processor-memory.ts
    line: 17
    actions:
      - "const result = await agent.generate('Share your contact info', {"
      - "const result = await agent.stream('Share your contact info', {"
      - "const result = await agent.generate('Say something', {"
      - "const result = await agent.generate('What is my card number?', {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: getRelevanceScore
    file: core/src/relevance/mastra-agent/index.ts
    line: 28
    actions:
      - "const responseText = await this.agent.generate(prompt);"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: getRelevanceScore
    file: rag/src/rerank/relevance/mastra-agent/index.ts
    line: 28
    actions:
      - "response = await this.agent.generate(prompt);"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: getStructuringStream
    file: core/src/processors/processors/structured-output.ts
    line: 198
    actions:
      - "return this.agent.stream(messages, {"
      - return this.structuringAgent.stream(
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: getTitlesCandidates
    file: rag/src/document/extractors/title.ts
    line: 153
    actions:
      - "const result = await miniAgent.generate(["
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: handleMessageSend
    file: server/src/server/handlers/a2a.ts
    line: 561
    actions:
      - "const result = await agent.generate([convertToCoreMessage(message)], {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: handleMessageStream
    file: server/src/server/handlers/a2a.ts
    line: 873
    actions:
      - "const result = await agent.stream([convertToCoreMessage(message)], {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: moderateContent
    file: core/src/processors/processors/moderation.ts
    line: 276
    actions:
      - "const response = await this.moderationAgent.generate(prompt, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: processInput
    file: agent-builder/src/processors/tool-summary.ts
    line: 64
    actions:
      - "const summaryPromise = this.summaryAgent.generate("
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: processWithContext
    file: codemod/src/test/__fixtures__/runtime-context.input.ts
    line: 46
    actions:
      - "return agent.generate(message, { runtimeContext });"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: processWithContext
    file: codemod/src/test/__fixtures__/runtime-context.output.ts
    line: 46
    actions:
      - "return agent.generate(message, { requestContext });"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: runDefaultCompletionCheck
    file: core/src/loop/network/validation.ts
    line: 386
    actions:
      - "const stream = await agent.stream(completionPrompt, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: runStreamUntilIdle
    file: core/src/agent/stream-until-idle.ts
    line: 526
    actions:
      - "opts => agent.stream(messages, opts as any) as Promise<MastraModelOutput<OUTPUT>>,"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: sendSignal
    file: core/src/agent/thread-stream-runtime.ts
    line: 387
    actions:
      - void agent
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: setupUseChatV5Plus
    file: memory/integration-tests/src/shared/useChat.ts
    line: 283
    actions:
      - "mastraServer = spawn("
      - "await agent.generate(`hi`, {"
    checks: []
    missing:
      - no input validation
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-03, ASI-04, ASI-06, ASI-10]

  - function: tryGenerateWithJsonFallback
    file: core/src/agent/utils.ts
    line: 27
    actions:
      - "return await agent.generate(prompt, options);"
      - "return await agent.generate(prompt, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: tryStreamWithJsonFallback
    file: core/src/agent/utils.ts
    line: 52
    actions:
      - "const result = await agent.stream(prompt, options);"
      - "return await agent.stream(prompt, {"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]


  # ⚠ NO CHECKS — destructive / http_write
  - function: checkMastraServerStatus
    file: agent-builder/src/defaults.ts
    line: 1416
    actions:
      - "const { stdout } = await execFile('lsof', ['-ti', String(port)]);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: commandExists
    file: core/src/workspace/sandbox/native-sandbox/detect.ts
    line: 15
    actions:
      - "execFileSync('which', [command], { stdio: 'ignore' });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: connectAndListTools
    file: playground/src/domains/mcps/hooks/use-try-connect-mcp.ts
    line: 36
    actions:
      - "const initResponse = await fetch(url, {"
      - "await fetch(url, {"
      - "const toolsResponse = await fetch(url, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: createChildProcessLogger
    file: deployer/src/deploy/log.ts
    line: 22
    actions:
      - "const subprocess = spawn(cmd, args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createIntrospectionValidator
    file: mcp/src/server/oauth-middleware.ts
    line: 289
    actions:
      - "const response = await fetch(introspectionEndpoint, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: createRunCommandTool
    file: core/src/loop/network/run-command-tool.ts
    line: 169
    actions:
      - "const { stdout, stderr } = await execAsync(command, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: exec
    file: core/src/storage/git-history.ts
    line: 183
    actions:
      - "execFile('git', args, { cwd, maxBuffer: 10 * 1024 * 1024 }, (error, stdout) => {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: executeCommand
    file: agent-builder/src/defaults.ts
    line: 2864
    actions:
      - "const { stdout, stderr } = await exec(command, execOptions);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: executeUpstashCommand
    file: loggers/src/upstash/index.ts
    line: 47
    actions:
      - "const response = await fetch(`${this.upstashUrl}/pipeline`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: execWithTimeout
    file: cli/src/commands/create/utils.ts
    line: 18
    actions:
      - "const promise = exec(command, { killSignal: 'SIGTERM' });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: fetchAnthropicAttachmentTokenEstimate
    file: memory/src/processors/observational-memory/token-counter.ts
    line: 1078
    actions:
      - "const response = await fetch('https://api.anthropic.com/v1/messages/count_tokens', {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: fetchGoogleAttachmentTokenEstimate
    file: memory/src/processors/observational-memory/token-counter.ts
    line: 1110
    actions:
      - "const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${modelId}:countTokens`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: fetchOpenAIAttachmentTokenEstimate
    file: memory/src/processors/observational-memory/token-counter.ts
    line: 1045
    actions:
      - "const response = await fetch('https://api.openai.com/v1/responses/input_tokens', {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: generateNpmLockfile
    file: deployer/src/bundler/index.ts
    line: 150
    actions:
      - "execSync('npm install --package-lock-only --force', {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: getAzureADToken
    file: core/src/llm/model/gateways/azure.ts
    line: 358
    actions:
      - "const response = await fetch(tokenEndpoint, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: getEntraIdToken
    file: core/src/llm/model/gateways/azure.ts
    line: 464
    actions:
      - this.entraIdTokenRequests.delete(cacheKey);
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: getExeca
    file: core/src/workspace/sandbox/execa.ts
    line: 12
    actions:
      - "loading = (async () => {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: getGitBranch
    file: cli/src/commands/studio/deploy.ts
    line: 40
    actions:
      - "return execSync('git rev-parse --abbrev-ref HEAD', {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: getPackageName
    file: cli/src/commands/server/deploy.ts
    line: 21
    actions:
      - "const raw = execSync('node -p \"require(\\'./package.json\\').name\"', {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: getPackageName
    file: cli/src/commands/studio/deploy.ts
    line: 26
    actions:
      - "const raw = execSync('node -p \"require(\\'./package.json\\').name\"', {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: getRelevanceScore
    file: rag/src/rerank/relevance/cohere/index.ts
    line: 28
    actions:
      - "const response = await fetch(`https://api.cohere.com/v2/rerank`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: getVersionTag
    file: cli/src/commands/utils.ts
    line: 84
    actions:
      - "const { stdout } = await execa('npm', ['dist-tag', 'ls', 'mastra'], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: github
    file: _changeset-cli/src/git/backport.ts
    line: 22
    actions:
      - "childProcess.execSync(`git fetch origin ${baseBranch}`, {"
      - "childProcess.execSync(`git switch \"${baseBranch}\"`, {"
      - "childProcess.execSync(`git pull origin \"${baseBranch}\"`, {"
      - "childProcess.execSync(`git branch -D \"${backportBranchName}\"`, {"
      - "childProcess.execSync(`git switch \"${backportBranchName}\"`, {"
      - "childProcess.execSync(`git cherry-pick --continue`, {"
      - "childProcess.execSync(`git checkout -b \"${backportBranchName}\"`, {"
      - "childProcess.execSync(`git cherry-pick -x ${commitSha}`, {"
      - "childProcess.execSync(`git push origin +${backportBranchName} --force`, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: gitInit
    file: cli/src/commands/utils.ts
    line: 118
    actions:
      - "await execa('git', ['init'], { cwd, stdio: 'ignore' });"
      - "await execa('git', ['add', '-A'], { cwd, stdio: 'ignore' });"
      - await execa(
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: initializePackageJson
    file: cli/src/commands/create/utils.ts
    line: 62
    actions:
      - await exec(initCommand);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: installDependencies
    file: cli/src/utils/clone-template.ts
    line: 163
    actions:
      - "await exec(installCommand, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: installMastraSkills
    file: cli/src/commands/init/skills-install.ts
    line: 3
    actions:
      - "await execa('npx', args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: installPackages
    file: cli/src/services/service.deps.ts
    line: 46
    actions:
      - "return execa(`${pm} ${installCommand} ${packageList}`, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: isDatadogTracerLikelyLoaded
    file: mcp/src/client/client.ts
    line: 127
    actions:
      - "if (process.execArgv.some(arg => arg.includes('dd-trace'))) {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: isGitInitialized
    file: cli/src/commands/utils.ts
    line: 106
    actions:
      - "await execa('git', ['rev-parse', '--is-inside-work-tree'], { cwd, stdio: 'ignore' });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: killProcessTree
    file: core/src/workspace/sandbox/local-process-manager.ts
    line: 167
    actions:
      - "const execa = await getExeca();"
      - "await execa('taskkill', ['/T', '/F', '/PID', String(pid)], { reject: false, stdio: 'ignore' });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: makeLogoutRequest
    file: playground/src/domains/auth/hooks/use-auth-actions.ts
    line: 119
    actions:
      - "const response = await fetch(`${baseUrl}${prefix}/auth/logout`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: openBrowser
    file: cli/src/commands/auth/credentials.ts
    line: 78
    actions:
      - "execFileSync('open', [url]);"
      - "execFileSync('cmd', ['/c', 'start', '', url]);"
      - "execFileSync('powershell.exe', ['-NoProfile', '-Command', `Start-Process '${url.replace(/'/g, \"''\")}'`]);"
      - "execFileSync('xdg-open', [url]);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: performSmartSearch
    file: agent-builder/src/defaults.ts
    line: 2498
    actions:
      - "const { stdout } = await execFile('rg', rgArgs, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: postTaskSnapshot
    file: server/src/server/a2a/push-notification-sender.ts
    line: 118
    actions:
      - "return this.options.fetch(requestUrl.toString(), {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: refreshSession
    file: playground/src/domains/auth/hooks/fetch-with-refresh.ts
    line: 7
    actions:
      - "const res = await fetch(`${baseUrl}${apiPrefix}/auth/refresh`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: registerUser
    file: mcp-docs-server/src/tools/course.ts
    line: 168
    actions:
      - "const response = await fetch('https://mastra.ai/api/course/register', {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: resetStorage
    file: playground/e2e/tests/__utils__/reset-storage.ts
    line: 4
    actions:
      - "return fetch(`${BASE_URL}/e2e/reset-storage`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: restartAllActiveWorkflowRuns
    file: cli/src/commands/dev/dev.ts
    line: 75
    actions:
      - "await fetch(`${scheme}://${host}:${port}/__restart-active-workflow-runs`, {"
      - "await fetch(`${scheme}://${host}:${port}/__restart-active-workflow-runs`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: runCommand
    file: server/scripts/check-core-imports.ts
    line: 84
    actions:
      - "return spawnSync(command, args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: seedDatasets
    file: playground/e2e/tests/__utils__/seed-datasets.ts
    line: 9
    actions:
      - "const res = await fetch(`${BASE_URL}/api/datasets`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: setupUseChatV4
    file: memory/integration-tests/src/shared/useChat.ts
    line: 53
    actions:
      - "mastraServer = spawn("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: spawn
    file: core/src/workspace/sandbox/local-process-manager.ts
    line: 195
    actions:
      - "const execa = await getExeca();"
      - "const subprocess = execa(wrapped.command, wrapped.args, execaOptions);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: spawnSWPM
    file: agent-builder/src/utils.ts
    line: 148
    actions:
      - "await spawn(swpmPath, [command, ...packageNames], { cwd });"
      - "await spawn(packageManager, args, { cwd });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: start
    file: cli/src/commands/start/start.ts
    line: 13
    actions:
      - "const server = spawn(process.execPath, commands, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: startWorker
    file: cli/src/commands/worker/start.ts
    line: 14
    actions:
      - "const child = spawn(process.execPath, ['index.mjs'], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: stopMastraServer
    file: agent-builder/src/defaults.ts
    line: 1297
    actions:
      - "const { stdout } = await execFile('lsof', ['-ti', String(port)]);"
      - "const { stdout: checkStdoutRaw } = await execFile('lsof', ['-ti', String(port)]);"
      - "const { stdout: finalCheckRaw } = await execFile('lsof', ['-ti', String(port)]);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: transform
    file: codemod/src/lib/transform.ts
    line: 96
    actions:
      - "const { stdout } = await exec(command, { encoding: 'utf8' });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: tryRefreshToken
    file: cli/src/commands/auth/credentials.ts
    line: 104
    actions:
      - "const res = await fetch(`${MASTRA_PLATFORM_API_URL}/v1/auth/refresh-token`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: updateCourseStateOnServer
    file: mcp-docs-server/src/tools/course.ts
    line: 264
    actions:
      - "const response = await fetch('https://mastra.ai/api/course/update', {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: useCloseBrowser
    file: playground/src/domains/agents/hooks/use-close-browser.ts
    line: 15
    actions:
      - "const response = await fetch(`/api/agents/${agentId}/browser/close`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: useCredentialsLogin
    file: playground/src/domains/auth/hooks/use-credentials-login.ts
    line: 56
    actions:
      - "const response = await fetch(`${baseUrl}/api/auth/credentials/sign-in`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: useCredentialsSignUp
    file: playground/src/domains/auth/hooks/use-credentials-signup.ts
    line: 59
    actions:
      - "const response = await fetch(`${baseUrl}/api/auth/credentials/sign-up`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: useInstallSkill
    file: playground/src/domains/workspace/hooks/use-skills-sh.ts
    line: 118
    actions:
      - "const response = await fetch(url, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: useRemoveSkill
    file: playground/src/domains/workspace/hooks/use-skills-sh.ts
    line: 201
    actions:
      - "const response = await fetch(url, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: useUpdateSkills
    file: playground/src/domains/workspace/hooks/use-skills-sh.ts
    line: 164
    actions:
      - "const response = await fetch(url, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: validate
    file: deployer/src/validator/validate.ts
    line: 65
    actions:
      - return spawn(
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: validateCodeCLI
    file: agent-builder/src/defaults.ts
    line: 1591
    actions:
      - "await execFile('npx', args, execOptions);"
      - "const { stdout } = await execFile('npx', eslintArgs, execOptions);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: validateESLintSingle
    file: agent-builder/src/defaults.ts
    line: 1882
    actions:
      - "const { stdout } = await execFile('npx', ['eslint', filePath, '--format', 'json'], { cwd: projectPath });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: whichSync
    file: core/src/workspace/lsp/servers.ts
    line: 19
    actions:
      - "execFileSync(cmd, [binary], { stdio: 'ignore' });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: writeAPIKey
    file: cli/src/commands/init/utils.ts
    line: 666
    actions:
      - "await exec(`echo ${escapedKey}=${escapedApiKey} >> ${envFileName}`);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]


  # ⚠ NO CHECKS — publish
  - function: #dispatch
    file: core/src/workflows/evented/workflow-event-processor/index.ts
    line: 2384
    actions:
      - "await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: #publishWorkflowStart
    file: core/src/workflows/scheduler/scheduler.ts
    line: 248
    actions:
      - "await this.#pubsub.publish(TOPIC_WORKFLOWS, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: abortHandler
    file: core/src/workflows/evented/workflow.ts
    line: 1689
    actions:
      - "this.mastra?.pubsub"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: cancel
    file: core/src/background-tasks/manager.ts
    line: 276
    actions:
      - "await this.pubsub.publish(TOPIC_DISPATCH, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: createOutputWriter
    file: core/src/workflows/evented/step-executor.ts
    line: 44
    actions:
      - "await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: createStepFromAgent
    file: core/src/workflows/workflow.ts
    line: 427
    actions:
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: dispatch
    file: core/src/background-tasks/manager.ts
    line: 693
    actions:
      - "await this.pubsub.publish(TOPIC_DISPATCH, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: emitChunkEvent
    file: core/src/agent/durable/stream-adapter.ts
    line: 303
    actions:
      - "await pubsub.publish(topic, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: emitErrorEvent
    file: core/src/agent/durable/stream-adapter.ts
    line: 360
    actions:
      - "await pubsub.publish(AGENT_STREAM_TOPIC(runId), {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: emitFinishEvent
    file: core/src/agent/durable/stream-adapter.ts
    line: 349
    actions:
      - "await pubsub.publish(AGENT_STREAM_TOPIC(runId), {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: emitIterationProgress
    file: core/src/workflows/handlers/control-flow.ts
    line: 931
    actions:
      - "pubsub.publish(`workflow.events.v2.${runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: emitStepFinishEvent
    file: core/src/agent/durable/stream-adapter.ts
    line: 334
    actions:
      - "await pubsub.publish(AGENT_STREAM_TOPIC(runId), {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: emitStepResultEvents
    file: core/src/workflows/handlers/step.ts
    line: 603
    actions:
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: emitStepStartEvent
    file: core/src/agent/durable/stream-adapter.ts
    line: 319
    actions:
      - "await pubsub.publish(AGENT_STREAM_TOPIC(runId), {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: emitSuspendedEvent
    file: core/src/agent/durable/stream-adapter.ts
    line: 377
    actions:
      - "await pubsub.publish(AGENT_STREAM_TOPIC(runId), {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: endWorkflow
    file: core/src/workflows/evented/workflow-event-processor/index.ts
    line: 337
    actions:
      - "await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await this.mastra.pubsub.publish('workflows', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: errorWorkflow
    file: core/src/workflows/evented/workflow-event-processor/index.ts
    line: 161
    actions:
      - "await this.mastra.pubsub.publish('workflows', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: execute
    file: core/src/workflows/default.ts
    line: 676
    actions:
      - "await params.pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await params.pubsub.publish(`workflow.events.v2.${runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: execute
    file: core/src/workflows/evented/execution-engine.ts
    line: 60
    actions:
      - "await pubsub.publish('workflows', {"
      - "await pubsub.publish('workflows', {"
      - "await pubsub.publish('workflows', {"
      - "await pubsub.publish('workflows', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: executeEntry
    file: core/src/workflows/handlers/entry.ts
    line: 216
    actions:
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: executeForeach
    file: core/src/workflows/handlers/control-flow.ts
    line: 826
    actions:
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: nestedWatchCb
    file: core/src/workflows/workflow.ts
    line: 3823
    actions:
      - "void this.pubsub.publish(`workflow.events.v2.${this.runId}`, {"
      - "void this.pubsub.publish(`workflow.events.v2.${this.runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: onStepExecutionStart
    file: core/src/workflows/default.ts
    line: 175
    actions:
      - "await params.pubsub.publish(`workflow.events.v2.${params.executionContext.runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: processAgentStream
    file: core/src/workflows/evented/workflow.ts
    line: 395
    actions:
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: processWorkflowConditional
    file: core/src/workflows/evented/workflow-event-processor/parallel.ts
    line: 81
    actions:
      - "await pubsub.publish('workflows', {"
      - "return pubsub.publish('workflows', {"
      - "return pubsub.publish('workflows', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: processWorkflowEnd
    file: core/src/workflows/evented/workflow-event-processor/index.ts
    line: 391
    actions:
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows-finish', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: processWorkflowFail
    file: core/src/workflows/evented/workflow-event-processor/index.ts
    line: 549
    actions:
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows-finish', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: processWorkflowForEach
    file: core/src/workflows/evented/workflow-event-processor/loop.ts
    line: 113
    actions:
      - "await pubsub.publish('workflows', {"
      - "await pubsub.publish('workflows', {"
      - "await pubsub.publish('workflows', {"
      - "await pubsub.publish('workflows', {"
      - "await pubsub.publish('workflows', {"
      - "await pubsub.publish('workflows', {"
      - "await pubsub.publish('workflows', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: processWorkflowLoop
    file: core/src/workflows/evented/workflow-event-processor/loop.ts
    line: 10
    actions:
      - "await pubsub.publish('workflows', { type: 'workflow.step.end', runId, data: loopEndData });"
      - "await pubsub.publish('workflows', { type: 'workflow.step.run', runId, data: loopAgainData });"
      - "await pubsub.publish('workflows', { type: 'workflow.step.run', runId, data: loopAgainData });"
      - "await pubsub.publish('workflows', { type: 'workflow.step.end', runId, data: loopEndData });"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: processWorkflowParallel
    file: core/src/workflows/evented/workflow-event-processor/parallel.ts
    line: 8
    actions:
      - "return pubsub.publish('workflows', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: processWorkflowSleep
    file: core/src/workflows/evented/workflow-event-processor/sleep.ts
    line: 50
    actions:
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish('workflows', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: processWorkflowSleepUntil
    file: core/src/workflows/evented/workflow-event-processor/sleep.ts
    line: 158
    actions:
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await pubsub.publish('workflows', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: processWorkflowStart
    file: core/src/workflows/evented/workflow-event-processor/index.ts
    line: 217
    actions:
      - "await this.mastra.pubsub.publish('workflows', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: processWorkflowSuspend
    file: core/src/workflows/evented/workflow-event-processor/index.ts
    line: 473
    actions:
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows-finish', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: processWorkflowWaitForEvent
    file: core/src/workflows/evented/workflow-event-processor/sleep.ts
    line: 8
    actions:
      - "await pubsub.publish('workflows', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: publishLifecycleEvent
    file: core/src/background-tasks/manager.ts
    line: 1048
    actions:
      - "await this.pubsub.publish(TOPIC_RESULT, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: publishSkillFromSource
    file: core/src/workspace/skills/publish.ts
    line: 260
    actions:
      - await blobStore.putMany(result.blobs);
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: resume
    file: core/src/background-tasks/manager.ts
    line: 368
    actions:
      - "await this.pubsub.publish(TOPIC_DISPATCH, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: setupAbortHandler
    file: core/src/workflows/evented/workflow.ts
    line: 1688
    actions:
      - "this.mastra?.pubsub"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: startAsync
    file: core/src/workflows/evented/workflow.ts
    line: 1794
    actions:
      - "await this.mastra.pubsub.publish('workflows', {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: stream
    file: core/src/workflows/workflow.ts
    line: 3566
    actions:
      - "void self.pubsub.publish(`workflow.events.v2.${self.runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: streamLegacy
    file: core/src/workflows/workflow.ts
    line: 3403
    actions:
      - "await this.pubsub.publish(`workflow.events.v2.${this.runId}`, {"
      - "void this.pubsub.publish(`workflow.events.v2.${this.runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]

  - function: watch
    file: core/src/workflows/workflow.ts
    line: 3816
    actions:
      - "void this.pubsub.publish(`workflow.events.v2.${this.runId}`, {"
      - "void this.pubsub.publish(`workflow.events.v2.${this.runId}`, {"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]


  # ~ PARTIAL CHECKS
  - function: aggregateBranchResults
    file: core/src/workflows/evented/workflow-event-processor/index.ts
    line: 1498
    actions:
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows', {"
    checks:
      - type: input_validation
        code: "if (finishedCount < branchEntry.steps.length)"
      - type: input_validation
        code: "if (suspendedCount > 0)"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02]

  - function: call
    file: memory/src/processors/observational-memory/reflector-runner.ts
    line: 235
    actions:
      - "const streamResult = await agent.stream(prompt, {"
    checks:
      - type: input_validation
        code: "if (chunkCount === 1 || chunkCount % 50 === 0)"
      - type: input_validation
        code: "if (!parsed.degenerate && (validateCompression(reflectedTokens, targetThreshold) || )"
      - type: input_validation
        code: "if (parsed.degenerate && currentLevel >= maxLevel)"
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: createFromTemplate
    file: cli/src/commands/create/create.ts
    line: 262
    actions:
      - "await fs.rm(projectPath, { recursive: true, force: true });"
    checks:
      - type: confirmation
        code: "const gitConfirmResult = await p.confirm({"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createMastraProject
    file: cli/src/commands/create/utils.ts
    line: 157
    actions:
      - "await exec(`${pm} ${installCommand} zod@^4`);"
      - "await exec(`${pm} ${installCommand} -D typescript @types/node`);"
      - "await exec(`echo '{"
      - "await exec(`echo output.txt >> .gitignore`);"
      - "await exec(`echo node_modules >> .gitignore`);"
      - "await exec(`echo dist >> .gitignore`);"
      - "await exec(`echo .mastra >> .gitignore`);"
      - "await exec(`echo .env.development >> .gitignore`);"
      - "await exec(`echo .env >> .gitignore`);"
      - "await exec(`echo *.db >> .gitignore`);"
      - "await exec(`echo *.db-* >> .gitignore`);"
      - "await exec(`echo .netlify >> .gitignore`);"
      - "await exec(`echo .vercel >> .gitignore`);"
      - "await fs.rm(projectPath, { recursive: true, force: true });"
    checks:
      - type: input_validation
        code: "if (!value || value.length === 0)"
      - type: input_validation
        code: if (fsSync.existsSync(value))
    missing:
      - no auth check
      - no confirmation step
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createNetworkLoop
    file: core/src/loop/network/index.ts
    line: 513
    actions:
      - ": agentForStep.stream(messagesForSubAgent, {"
    checks:
      - type: approval_step
        code: "if (chunk.type === 'tool-call-approval')"
      - type: approval_step
        code: "if (chunk.payload.result === 'Tool call was not approved by the user')"
      - type: approval_step
        code: "if (requireApprovalMetadata || suspendedTools)"
      - type: approval_step
        code: if ((tool as any).needsApprovalFn)
      - type: approval_step
        code: if (toolRequiresApproval)
      - type: approval_step
        code: "if (!resumeData.approved)"
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: createProjectByName
    file: cli/src/commands/init/observability-provision.ts
    line: 174
    actions:
      - "const res = await platformFetch(`${MASTRA_PLATFORM_API_URL}/v1/studio/projects`, {"
    checks:
      - type: auth_check
        code: "headers: { ...authHeaders(token, orgId), 'Content-Type': 'application/json' },"
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: createToken
    file: cli/src/commands/auth/tokens.ts
    line: 16
    actions:
      - "const resp = await platformFetch(`${MASTRA_PLATFORM_API_URL}/v1/auth/tokens`, {"
    checks:
      - type: auth_check
        code: "...authHeaders(token, orgId),"
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: execute
    file: core/src/workflows/workflow.ts
    line: 2490
    actions:
      - "void pubsub.publish('nested-watch', {"
    checks:
      - type: input_validation
        code: "if (retryCount && retryCount > 0 && isResume && requestContext)"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02]

  - function: executeStep
    file: core/src/worker/strategies/http-remote-strategy.ts
    line: 57
    actions:
      - "const res = await fetch(url, {"
    checks:
      - type: auth_check
        code: "...this.#buildAuthHeaders(),"
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: initialize
    file: acp/src/connection.ts
    line: 223
    actions:
      - "this.agentProcess = spawn(this.options.command, this.options.args ?? [], {"
    checks:
      - type: auth_check
        code: "await this.connection.authenticate({ methodId: this.options.authMethodId });"
    missing:
      - no input validation
    owasp: [ASI-02, ASI-03]

  - function: listAgentTools
    file: core/src/agent/agent.ts
    line: 3664
    actions:
      - requestContext.delete(MASTRA_THREAD_ID_KEY);
      - requestContext.delete(MASTRA_RESOURCE_ID_KEY);
      - ": await resolvedAgent.generate(messagesForSubAgent, {"
      - ": await resolvedAgent.stream(messagesForSubAgent, {"
    checks:
      - type: input_validation
        code: "if (startResult.modifiedMaxSteps !== undefined)"
      - type: approval_step
        code: "if (chunk.type === 'data-tool-call-approval')"
      - type: approval_step
        code: "if (chunk.type === 'tool-call-approval')"
      - type: approval_step
        code: "if (requireToolApproval || suspendedPayload || resumeSchema)"
    missing:
      - no rate limit
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: migrate
    file: cli/src/commands/migrate/migrate.ts
    line: 28
    actions:
      - "const migrationProcess = execa(process.execPath, [join(dotMastraPath, 'output', 'index.mjs')], {"
    checks:
      - type: confirmation
        code: "const confirmed = await p.confirm({"
      - type: confirmation
        code: "if (p.isCancel(confirmed) || !confirmed)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: mintOrgToken
    file: cli/src/commands/init/observability-provision.ts
    line: 198
    actions:
      - "const res = await platformFetch(`${MASTRA_PLATFORM_API_URL}/v1/auth/tokens`, {"
    checks:
      - type: auth_check
        code: "headers: { ...authHeaders(token, orgId), 'Content-Type': 'application/json' },"
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: processChatMessage
    file: core/src/channels/agent-channels.ts
    line: 890
    actions:
      - "const stream = await agent.stream(streamInput, {"
    checks:
      - type: input_validation
        code: "if (maxMessages > 0 && !sdkThread.isDM)"
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: processWorkflowStepEnd
    file: core/src/workflows/evented/workflow-event-processor/index.ts
    line: 1647
    actions:
      - "await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows', {"
    checks:
      - type: input_validation
        code: "if (pendingCount > 0)"
      - type: input_validation
        code: "if (suspendedCount > 0)"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02]

  - function: processWorkflowStepRun
    file: core/src/workflows/evented/workflow-event-processor/index.ts
    line: 629
    actions:
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {"
      - "return this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows', {"
      - "return this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows', {"
      - "await this.mastra.pubsub.publish('workflows', {"
    checks:
      - type: input_validation
        code: "if (retryCount >= retries)"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02]

  - function: revokeTokenAction
    file: cli/src/commands/auth/tokens.ts
    line: 82
    actions:
      - "const resp = await platformFetch(`${MASTRA_PLATFORM_API_URL}/v1/auth/tokens/${encodeURIComponent(tokenId)}`, {"
    checks:
      - type: auth_check
        code: "headers: authHeaders(token, orgId),"
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: setupLLMRecording
    file: _llm-recorder/src/llm-recorder.ts
    line: 739
    actions:
      - fs.unlinkSync(recordingPath);
    checks:
      - type: input_validation
        code: if (isStreaming)
      - type: input_validation
        code: if (recording.response.isStreaming)
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: startDeployDiagnosis
    file: cli/src/commands/studio/platform-api.ts
    line: 105
    actions:
      - "const resp = await platformFetch(`${MASTRA_PLATFORM_API_URL}/v1/studio/deploys/${deployId}/diagnosis`, {"
    checks:
      - type: auth_check
        code: "headers: authHeaders(token, orgId),"
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: startServer
    file: cli/src/commands/dev/dev.ts
    line: 95
    actions:
      - "currentServerProcess = execa(process.execPath, commands, {"
      - "await fetch(`${scheme}://${host}:${port}${studioBasePath}/__refresh`, {"
      - "await fetch(`${scheme}://${host}:${port}${studioBasePath}/__refresh`, {"
    checks:
      - type: input_validation
        code: "if (errorRestartCount > ON_ERROR_MAX_RESTARTS)"
    missing:
      - no auth check
      - no rate limit
    owasp: [ASI-01, ASI-02, ASI-03, ASI-06]

  - function: startServerDeployDiagnosis
    file: cli/src/commands/server/platform-api.ts
    line: 102
    actions:
      - "const resp = await platformFetch(`${MASTRA_PLATFORM_API_URL}/v1/server/deploys/${deployId}/diagnosis`, {"
    checks:
      - type: auth_check
        code: "headers: authHeaders(token, orgId),"
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: uploadDeploy
    file: cli/src/commands/studio/platform-api.ts
    line: 126
    actions:
      - "const uploadResp = await fetch(uploadUrl, {"
    checks:
      - type: confirmation
        code: "await confirmUploadWithRetry({"
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: uploadServerDeploy
    file: cli/src/commands/server/platform-api.ts
    line: 123
    actions:
      - "const uploadResp = await fetch(uploadUrl, {"
    checks:
      - type: confirmation
        code: "await confirmUploadWithRetry({"
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

