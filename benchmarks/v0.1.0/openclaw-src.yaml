# toolcalls.yaml
# What your agent can do to the real world — and what checks exist.
# Generated by diplomat-agent-ts · https://diplomat.run
#
# Commit this file. Review changes in PRs.
# In source code:
#   Fix          → add validation, the next scan picks it up
#   Acknowledge  → add  # checked:ok  to confirm you've reviewed it
#   Protected elsewhere → add  # checked:ok — protected by [middleware/gateway/etc]
#   CI enforcement → --fail-on-unchecked blocks PRs with new unreviewed tool calls

generated: "2026-05-15T11:53:39.240Z"
version: "0.1.0"
spec_version: "1.0"
path: "/Users/josselin/dev/diplomat-openclaw-main/src"
language: typescript

summary:
  total: 418
  no_checks: 331
  partial_checks: 87
  confirmed: 0

tool_calls:

  # ⚠ NO CHECKS — database_delete / file_delete
  - function: acquireClobberLock
    file: config/io.clobber-snapshot.ts
    line: 73
    actions:
      - "await deps.fs.promises.rmdir(lockPath).catch(() => {});"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: acquireClobberLockSync
    file: config/io.clobber-snapshot.ts
    line: 97
    actions:
      - deps.fs.rmdirSync(lockPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: acquireGatewayLock
    file: infra/gateway-lock.ts
    line: 246
    actions:
      - "await fs.rm(lockPath, { force: true });"
      - "await fs.rm(lockPath, { force: true });"
      - "await fs.rm(lockPath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: acquireUsageCostCacheRefreshLock
    file: infra/session-cost-usage.ts
    line: 273
    actions:
      - "await fs.promises.rm(lockPath, { force: true }).catch(() => undefined);"
      - "await fs.promises.rm(lockPath, { force: true }).catch(() => undefined);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: applyPluginUninstallDirectoryRemoval
    file: plugins/uninstall.ts
    line: 575
    actions:
      - "await fs.rm(removal.target, { recursive: true, force: true });"
      - await fs.rmdir(removal.cleanup.parentDir);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: bootstrapPnpmViaNpm
    file: infra/update-package-manager.ts
    line: 116
    actions:
      - "await fs.rm(tempRoot, { recursive: true, force: true }).catch(() => {});"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: broadcastInternal
    file: gateway/server-broadcast.ts
    line: 91
    actions:
      - reportedSlowPayloadClients.delete(c);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanOrphanBackups
    file: config/backup-rotation.ts
    line: 72
    actions:
      - "await ioFs.unlink(path.join(dir, entry)).catch(() => {"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanStaleLockFiles
    file: agents/session-write-lock.ts
    line: 548
    actions:
      - "await fs.rm(lockPath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanup
    file: infra/update-package-manager.ts
    line: 122
    actions:
      - "await fs.rm(tempRoot, { recursive: true, force: true }).catch(() => {});"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanup
    file: security/test-temp-cases.ts
    line: 15
    actions:
      - "await fs.rm(this.fixtureRoot, { recursive: true, force: true }).catch(() => undefined);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanup
    file: plugins/test-helpers/fs-fixtures.ts
    line: 77
    actions:
      - "fs.rmSync(suiteTempRoot, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupArchivedSessionTranscripts
    file: gateway/session-transcript-files.fs.ts
    line: 243
    actions:
      - "await fs.promises.rm(fullPath).catch(() => undefined);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupCompactionCheckpointSnapshot
    file: gateway/session-compaction-checkpoints.ts
    line: 353
    actions:
      - await fs.unlink(snapshot.sessionFile);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupEmbeddedPiRunnerTestWorkspace
    file: agents/test-helpers/pi-embedded-runner-e2e-fixtures.ts
    line: 26
    actions:
      - "await fs.rm(workspace.tempRoot, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupFailedSpawnBeforeAgentStart
    file: agents/subagent-spawn.ts
    line: 520
    actions:
      - "await fs.rm(params.attachmentAbsDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupGlobalRenameDirs
    file: infra/update-global.ts
    line: 744
    actions:
      - "await fs.rm(target, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupInstallTempDir
    file: infra/install-package-dir.ts
    line: 123
    actions:
      - "await fs.rm(dirPath, { recursive: true, force: true }).catch(() => undefined);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupLegacyPluginDependencyState
    file: commands/doctor/shared/plugin-dependency-cleanup.ts
    line: 119
    actions:
      - "await fs.rm(target, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupManagedOutgoingImageRecords
    file: gateway/managed-image-attachments.ts
    line: 424
    actions:
      - "await fs.rm(recordPath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupPluginLoaderFixturesForTest
    file: plugins/loader.test-fixtures.ts
    line: 155
    actions:
      - "fs.rmSync(fixtureRoot, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupTempDirs
    file: plugin-sdk/qa-runtime.test-helpers.ts
    line: 12
    actions:
      - "fs.rmSync(dir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupTempPaths
    file: agents/pi-embedded-runner/run/attempt.spawn-workspace.test-support.ts
    line: 953
    actions:
      - "await fs.rm(target, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupTempPluginTestEnvironment
    file: agents/test-helpers/temp-plugin-extension-fixtures.ts
    line: 49
    actions:
      - "fs.rmSync(dir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupTrackedTempDirs
    file: plugins/test-helpers/fs-fixtures.ts
    line: 32
    actions:
      - "fs.rmSync(dir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupTrackedTempDirsAsync
    file: plugins/test-helpers/fs-fixtures.ts
    line: 42
    actions:
      - "await fsPromises.rm(dir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupTrimmedCompactionCheckpointFiles
    file: gateway/session-compaction-checkpoints.ts
    line: 366
    actions:
      - await fs.unlink(resolvedSessionFile);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cloneMarketplaceRepo
    file: plugins/marketplace.ts
    line: 424
    actions:
      - "await fs.rm(tmpDir, { recursive: true, force: true }).catch(() => undefined);"
      - "await fs.rm(tmpDir, { recursive: true, force: true }).catch(() => undefined);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: continuePostCoreUpdateInFreshProcess
    file: cli/update-cli/update-command.ts
    line: 1849
    actions:
      - "const child = spawn(resolveNodeRunner(), argv, {"
      - "await fs.rm(resultDir, { recursive: true, force: true }).catch(() => undefined);"
    checks: []
    missing:
      - no input validation
      - no auth check
      - no confirmation step
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createBundleMcpTempHarness
    file: plugins/bundle-mcp.test-support.ts
    line: 6
    actions:
      - "tempDirs.splice(0).map((dir) => fs.rm(dir, { recursive: true, force: true })),"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createCommandWorkspaceHarness
    file: auto-reply/reply/commands-filesystem.test-support.ts
    line: 5
    actions:
      - "tempDirs.splice(0).map((dir) => fs.rm(dir, { recursive: true, force: true })),"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createCronStoreHarness
    file: cron/service.test-harness.ts
    line: 27
    actions:
      - "await fs.rm(fixtureRoot, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createFixtureSuite
    file: test-utils/fixture-suite.ts
    line: 5
    actions:
      - "await fs.rm(fixtureRoot, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createGatewayBroadcaster
    file: gateway/server-broadcast.ts
    line: 87
    actions:
      - reportedSlowPayloadClients.delete(c);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createRebindableDirectoryAlias
    file: test-utils/symlink-rebind-race.ts
    line: 5
    actions:
      - "await fs.rm(aliasPath, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createSandboxFsBridgeFromResolver
    file: agents/test-helpers/host-sandbox-fs-bridge.ts
    line: 6
    actions:
      - "await fs.rm(target.hostPath, {"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createSshSandboxSessionFromSettings
    file: agents/sandbox/ssh.ts
    line: 135
    actions:
      - "await fs.rm(configDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createSuiteTempRootTracker
    file: plugins/test-helpers/fs-fixtures.ts
    line: 54
    actions:
      - "fs.rmSync(suiteTempRoot, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createTempHomeEnv
    file: test-utils/temp-home.ts
    line: 44
    actions:
      - "await fs.rm(home, { recursive: true, force: true });"
      - "await fs.rm(home, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createTempHomeHarness
    file: auto-reply/reply.test-harness.ts
    line: 155
    actions:
      - "await fs.rm(fixtureRoot, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createTrackedTempDirs
    file: test-utils/tracked-temp-dirs.ts
    line: 5
    actions:
      - "await fs.rm(path.join(dir, entry), { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: deleteManagedImageRecordArtifacts
    file: gateway/managed-image-attachments.ts
    line: 363
    actions:
      - "await fs.rm(filePath, { force: true });"
      - "await fs.rm(resolveOutgoingRecordPath(record.attachmentId, stateDir), { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: deleteOrphanManagedImageFiles
    file: gateway/managed-image-attachments.ts
    line: 388
    actions:
      - "await fs.rm(filePath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: deleteSessions
    file: proxy-capture/store.sqlite.ts
    line: 392
    actions:
      - "fs.rmSync(blobPath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: disposeSshSandboxSession
    file: agents/sandbox/ssh.ts
    line: 205
    actions:
      - "await fs.rm(path.dirname(session.configPath), { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: downloadFile
    file: agents/skills-install-download.ts
    line: 73
    actions:
      - "await fs.promises.rm(tempPath, { force: true }).catch(() => undefined);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: ensureOpenClawPluginSdkAlias
    file: plugins/plugin-sdk-dist-alias.ts
    line: 23
    actions:
      - "fs.rmSync(pluginSdkAliasDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: forkCompactionCheckpointTranscriptAsync
    file: gateway/session-compaction-checkpoints.ts
    line: 242
    actions:
      - await fs.unlink(sessionFile);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: generateSlugViaLLM
    file: hooks/llm-slug-generator.ts
    line: 34
    actions:
      - "await fs.rm(path.dirname(tempSessionFile), { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: hideProjectNpmConfigForInstall
    file: infra/install-package-dir.ts
    line: 57
    actions:
      - "await fs.rm(hiddenDir, { recursive: true, force: true }).catch(() => undefined);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: installDeliveryQueueTmpDirHooks
    file: infra/outbound/delivery-queue.test-helpers.ts
    line: 7
    actions:
      - "fs.rmSync(fixtureRoot, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: installGeneratedPluginTempRootCleanup
    file: plugins/generated-plugin-test-helpers.ts
    line: 21
    actions:
      - "fs.rmSync(dir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: installPackageDir
    file: infra/install-package-dir.ts
    line: 147
    actions:
      - "await fs.rm(backupDir, { recursive: true, force: true }).catch(() => undefined);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: isUsageCostCacheRefreshRunning
    file: infra/session-cost-usage.ts
    line: 252
    actions:
      - "await fs.promises.rm(lockPath, { force: true }).catch(() => undefined);"
      - "await fs.promises.rm(lockPath, { force: true }).catch(() => undefined);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: linkOpenClawPeerDependencies
    file: plugins/plugin-peer-link.ts
    line: 85
    actions:
      - "await fs.rm(linkPath, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: migrateLegacySessions
    file: infra/state-migrations.ts
    line: 777
    actions:
      - "fs.rmSync(detected.sessions.legacyStorePath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: migrateMonolithicIfNeeded
    file: agents/sandbox/registry.ts
    line: 230
    actions:
      - "await fs.rm(registryPath, { force: true });"
      - "await fs.rm(registryPath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: moveLegacyRootMemoryFileToArchive
    file: commands/doctor-workspace.ts
    line: 170
    actions:
      - await fs.promises.unlink(params.legacyPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: packToArchive
    file: plugins/test-helpers/archive-fixtures.ts
    line: 5
    actions:
      - "fs.rmSync(dest, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: pruneOldBundles
    file: logging/diagnostic-stability-bundle.ts
    line: 579
    actions:
      - fs.unlinkSync(entry.file);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: pruneOldRollingLogs
    file: logging/logger.ts
    line: 725
    actions:
      - "fs.rmSync(fullPath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: publishTempArchive
    file: infra/backup-create.ts
    line: 270
    actions:
      - "await fs.rm(params.outputPath, { force: true }).catch(() => undefined);"
      - "await fs.rm(params.tempArchivePath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: purgeAll
    file: proxy-capture/store.sqlite.ts
    line: 374
    actions:
      - "fs.rmSync(path.join(this.blobDir, entry), { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: quarantineLegacyRegistry
    file: agents/sandbox/registry.ts
    line: 219
    actions:
      - "await fs.rm(registryPath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readFileUtf8AndCleanup
    file: test-utils/camera-url-test-helpers.ts
    line: 13
    actions:
      - "await fs.unlink(filePath).catch(() => {});"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readPending
    file: crestodian/rescue-message.ts
    line: 80
    actions:
      - "await fs.rm(pendingPath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readRestartSentinel
    file: infra/restart-sentinel.ts
    line: 175
    actions:
      - "await fs.unlink(filePath).catch(() => {});"
      - "await fs.unlink(filePath).catch(() => {});"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: reconcileWorkspaceBootstrapCompletionState
    file: agents/workspace.ts
    line: 263
    actions:
      - "await fs.rm(params.bootstrapPath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: relaunchGatewayScheduledTask
    file: infra/windows-task-restart.ts
    line: 62
    actions:
      - "const child = spawn(\"cmd.exe\", [\"/d\", \"/s\", \"/c\", quotedScriptPath], {"
      - fs.unlinkSync(scriptPath);
    checks: []
    missing:
      - no input validation
      - no auth check
      - no confirmation step
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeDirIfEmpty
    file: infra/state-migrations.ts
    line: 405
    actions:
      - fs.rmdirSync(dir);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeFileIfExists
    file: config/sessions/disk-budget.ts
    line: 241
    actions:
      - "await fs.promises.rm(filePath, { force: true }).catch(() => undefined);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeGeneratedPluginSkillEntry
    file: agents/skills/plugin-skills.ts
    line: 266
    actions:
      - "fs.rmSync(linkPath, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeManagedNpmDependency
    file: commands/doctor-plugin-registry.ts
    line: 122
    actions:
      - "fs.rmSync(params.packageDir, { recursive: true, force: true });"
      - fs.rmdirSync(scopeDir);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeOAuthTestTempRoot
    file: agents/auth-profiles/oauth-test-utils.ts
    line: 72
    actions:
      - "await fs.rm(tempRoot, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removePath
    file: commands/cleanup-utils.ts
    line: 78
    actions:
      - "await fs.rm(resolved, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeRecordFiles
    file: gateway/server-methods/skills-upload-store.ts
    line: 264
    actions:
      - "await fs.rm(resolveIdempotencyPath(rootDir, record.idempotencyKeyHash), { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeRegularFile
    file: trajectory/cleanup.ts
    line: 129
    actions:
      - "await fs.promises.rm(filePath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeReportedStaleLockIfStillStale
    file: agents/session-write-lock.ts
    line: 458
    actions:
      - "await fs.rm(params.lockPath, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeRestartSentinelFile
    file: infra/restart-sentinel.ts
    line: 155
    actions:
      - "await fs.unlink(filePath).catch(() => {});"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeShardedEntry
    file: agents/sandbox/registry.ts
    line: 179
    actions:
      - "await fs.rm(shardedEntryFilePath(dir, containerName), { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeStalePluginRuntimeSymlinks
    file: commands/doctor/shared/plugin-runtime-symlinks.ts
    line: 122
    actions:
      - await fsApi.unlink(item.path);
      - "await fsApi.rm(item.path, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeTempPlannerDir
    file: crestodian/assistant.ts
    line: 249
    actions:
      - "await fs.rm(dir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeUploadDir
    file: gateway/server-methods/skills-upload-store.ts
    line: 260
    actions:
      - "await fs.rm(resolveUploadDir(rootDir, uploadId), { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: renameExecApprovalsWithFallback
    file: infra/exec-approvals.ts
    line: 484
    actions:
      - assertSafeExecApprovalsOverwriteFallback(filePath);
      - "copyExecApprovalsFallback(tempPath, filePath);"
      - "fs.rmSync(tempPath, { force: true });"
    checks: []
    missing:
      - no input validation
      - no auth check
      - no confirmation step
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resetBackupTempHome
    file: commands/backup.test-support.ts
    line: 31
    actions:
      - "await fs.rm(tempHome.home, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resetMatrixSessionBindingStateDir
    file: channels/plugins/contracts/test-helpers/registry-session-binding.ts
    line: 96
    actions:
      - "fs.rmSync(matrixSessionBindingStateDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resetReplyRunSession
    file: auto-reply/reply/agent-runner-session-reset.ts
    line: 38
    actions:
      - fs.unlinkSync(candidate);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolvePatchFileOps
    file: agents/apply-patch.ts
    line: 231
    actions:
      - await fs.rm(filePath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: restoreExecApprovalsFallbackDestination
    file: infra/exec-approvals.ts
    line: 440
    actions:
      - "fs.rmSync(filePath, { force: true });"
      - "writeExecApprovalsFallbackBuffer(destination.fd, destination.snapshot ?? Buffer.alloc(0));"
    checks: []
    missing:
      - no confirmation step
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: restoreExecApprovalsSnapshot
    file: infra/exec-approvals.ts
    line: 733
    actions:
      - "fs.rmSync(snapshot.path, { force: true });"
      - "writeExecApprovalsRaw(snapshot.path, snapshot.raw);"
      - saveExecApprovals(snapshot.file);
    checks: []
    missing:
      - no confirmation step
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: restoreFileSnapshot
    file: secrets/apply.ts
    line: 739
    actions:
      - "fs.rmSync(pathname, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: restoreFileSnapshotSync
    file: config/io.ts
    line: 1300
    actions:
      - deps.fs.unlinkSync(filePath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: restoreProjectNpmConfigAfterInstall
    file: infra/install-package-dir.ts
    line: 76
    actions:
      - "await fs.rm(hiddenConfig.hiddenDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: rollbackManagedNpmPluginInstall
    file: plugins/install.ts
    line: 319
    actions:
      - "await fs.rm(params.targetDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: rollbackShippedPluginInstallConfigWriteMigration
    file: config/io.ts
    line: 1477
    actions:
      - deps.fs.unlinkSync(migration.filePath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: rotateLogFile
    file: logging/logger.ts
    line: 757
    actions:
      - "fs.rmSync(rotatedLogPath(file, MAX_ROTATED_LOG_FILES), { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runCliEntry
    file: media-understanding/runner.entries.ts
    line: 732
    actions:
      - "await fs.rm(outputDir, { recursive: true, force: true }).catch(() => {});"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runSessionsJson
    file: commands/sessions.test-helpers.ts
    line: 74
    actions:
      - "fs.rmSync(store, { force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: safeRemoveAttachmentsDir
    file: agents/subagent-registry-helpers.ts
    line: 188
    actions:
      - "await fs.rm(dirBase, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: safeRemoveAttachmentsDirSync
    file: agents/subagent-registry-helpers.ts
    line: 224
    actions:
      - "fsSync.rmSync(dirReal, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: scrubManagedNpmRootOpenClawPeer
    file: infra/npm-managed-root.ts
    line: 322
    actions:
      - "await fs.rm(openclawPackageDir, { recursive: true, force: true });"
      - "fs.rm(path.join(binDir, binName), { force: true }),"
      - "await fs.rm(path.join(params.npmRoot, \"node_modules\", \".package-lock.json\"), {"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: setupGatewaySessionsTestHarness
    file: gateway/test/server-sessions.test-helpers.ts
    line: 243
    actions:
      - "await fs.rm(sharedSessionStoreDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: stageRemoteFileIntoRoot
    file: auto-reply/reply/stage-sandbox-media.ts
    line: 150
    actions:
      - "await fs.rm(tmpDir, { recursive: true, force: true }).catch(() => {});"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: syncSkillsToWorkspace
    file: agents/skills/workspace.ts
    line: 1121
    actions:
      - "await fsp.rm(targetSkillsDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: uninstallLegacySystemdUnits
    file: daemon/systemd.ts
    line: 940
    actions:
      - await fs.unlink(unit.unitPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: uninstallScheduledTask
    file: daemon/schtasks.ts
    line: 906
    actions:
      - await fs.unlink(startupEntryPath);
      - await fs.unlink(scriptPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: uninstallSystemdService
    file: daemon/systemd.ts
    line: 776
    actions:
      - await fs.unlink(unitPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: unlinkGatewayRestartIntentFileSync
    file: infra/restart.ts
    line: 107
    actions:
      - fs.unlinkSync(intentPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: unlinkIfExists
    file: media/temp-files.ts
    line: 3
    actions:
      - await fs.unlink(filePath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: unlinkRegularFileSync
    file: infra/restart-handoff.ts
    line: 94
    actions:
      - fs.unlinkSync(filePath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: useTempSessionsFixture
    file: config/sessions/test-helpers.ts
    line: 6
    actions:
      - "fs.rmSync(tempDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: withMediaFixture
    file: media-understanding/runner.test-utils.ts
    line: 14
    actions:
      - "await fs.unlink(tmpPath).catch(() => {});"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: withStateDirEnv
    file: test-helpers/state-dir-env.ts
    line: 19
    actions:
      - "await fs.rm(tempRoot, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: withTempDir
    file: test-utils/temp-dir.ts
    line: 5
    actions:
      - "await fs.rm(dir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: withTempDir
    file: agents/sandbox/fs-bridge.test-helpers.ts
    line: 156
    actions:
      - "await fs.rm(stateDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: withTempPdfAgentDir
    file: agents/tools/pdf-tool.test-support.ts
    line: 6
    actions:
      - "await fs.rm(agentDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: withTempSecretFiles
    file: test-utils/secret-file-fixture.ts
    line: 10
    actions:
      - "await fs.rm(dir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: withUnsafeMountedSandboxHarness
    file: agents/test-helpers/unsafe-mounted-sandbox.ts
    line: 68
    actions:
      - "await fs.rm(stateDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: withWindowsEnv
    file: daemon/test-helpers/schtasks-fixtures.ts
    line: 16
    actions:
      - "await fs.rm(tmpDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: writeExecApprovalsRaw
    file: infra/exec-approvals.ts
    line: 707
    actions:
      - assertSafeExecApprovalsDestination(filePath);
      - "renameExecApprovalsWithFallback(tempPath, filePath);"
      - "fs.rmSync(tempPath, { force: true });"
    checks: []
    missing:
      - no input validation
      - no auth check
      - no confirmation step
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: writeGeminiSystemSettings
    file: agents/cli-runner/bundle-mcp-gemini.ts
    line: 57
    actions:
      - "await fs.rm(tempDir, { recursive: true, force: true });"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: writeLaunchAgentPlist
    file: daemon/launchd.ts
    line: 661
    actions:
      - await fs.unlink(legacyPlistPath);
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: writeUsageCostCacheLockAtomically
    file: infra/session-cost-usage.ts
    line: 229
    actions:
      - "await fs.promises.rm(tempPath, { force: true }).catch(() => undefined);"
    checks: []
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]


  # ⚠ NO CHECKS — agent_invocation / llm_call
  - function: consultRealtimeVoiceAgent
    file: talk/agent-consult-runtime.ts
    line: 182
    actions:
      - "const result = await params.agentRuntime.runEmbeddedPiAgent({"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: createOpenAICompletionsTransportStreamFn
    file: agents/openai-transport-stream.ts
    line: 1293
    actions:
      - "void (async () => {"
      - "const responseStream = (await client.chat.completions.create("
    checks: []
    missing:
      - no rate limit
      - no retry bound
    owasp: [ASI-02, ASI-05, ASI-06]

  - function: resolvePluginRuntime
    file: plugins/registry.ts
    line: 2239
    actions:
      - "run: (params) => withPluginRuntimePluginIdScope(pluginId, () => subagent.run(params)),"
    checks: []
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]


  # ⚠ NO CHECKS — destructive / http_write
  - function: addAllowlistEntry
    file: infra/exec-approvals.ts
    line: 1131
    actions:
      - saveExecApprovals(approvals);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: anthropicAnalyzePdf
    file: agents/tools/pdf-native-providers.ts
    line: 39
    actions:
      - "const res = await fetch(`${baseUrl}/v1/messages`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: applySessionsPatchToStore
    file: gateway/sessions-patch.ts
    line: 89
    actions:
      - "const normalized = normalizeExecAsk(raw);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: assertSafeExecApprovalsOverwriteFallback
    file: infra/exec-approvals.ts
    line: 306
    actions:
      - assertSafeExecApprovalsDestination(filePath);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildAcpPermissionRequest
    file: acp/permission-relay.ts
    line: 115
    actions:
      - "const decisions = normalizeGatewayExecApprovalDecisions(params.details?.allowedDecisions);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildAgentFollowupArgs
    file: agents/bash-tools.exec-approval-followup.ts
    line: 145
    actions:
      - "message: buildExecApprovalFollowupPrompt(params.resultText),"
      - "buildExecApprovalFollowupIdempotencyKey({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildApprovalInteractiveReply
    file: infra/exec-approval-reply.ts
    line: 172
    actions:
      - "buildExecApprovalActionDescriptors({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildApprovalPendingMessage
    file: agents/bash-tools.exec-runtime.ts
    line: 361
    actions:
      - "const allowedDecisions = params.allowedDecisions ?? resolveExecApprovalAllowedDecisions();"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildExecApprovalPendingReplyPayload
    file: infra/exec-approval-reply.ts
    line: 287
    actions:
      - "const descriptors = buildExecApprovalActionDescriptors({"
      - "`Expires in: ${formatExecApprovalExpiresIn(params.expiresAtMs, params.nowMs ?? Date.now())}`,"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildExecApprovalPendingToolResult
    file: agents/bash-tools.exec-host-shared.ts
    line: 447
    actions:
      - "const allowedDecisions = params.allowedDecisions ?? resolveExecApprovalAllowedDecisions();"
      - "? (buildExecApprovalUnavailableReplyPayload({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildExecApprovalRequestMessage
    file: infra/exec-approval-forwarder.ts
    line: 234
    actions:
      - "const allowedDecisions = resolveExecApprovalRequestAllowedDecisions(request.request);"
      - ".map((line) => sanitizeExecApprovalWarningText(line).trim())"
      - "resolveExecApprovalCommandDisplay(request.request).commandText,"
      - "lines.push(`Expires in: ${formatExecApprovalExpiresIn(request.expiresAtMs, nowMs)}`);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildExecApprovalUnavailableReplyPayload
    file: infra/exec-approval-reply.ts
    line: 355
    actions:
      - lines.push(getExecApprovalApproverDmNoticeText());
      - "channel && params.channelLabel && supportsNativeExecApprovalClient(channel)"
      - "? describeNativeExecApprovalClientSetup({"
      - lines.push(buildGenericNativeExecApprovalFallbackText());
      - "buildGenericNativeExecApprovalFallbackText({"
      - "`${buildGenericNativeExecApprovalFallbackText()} Then retry the command. You can usually leave execApprovals.approvers u"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildExecPendingPayload
    file: infra/exec-approval-forwarder.ts
    line: 414
    actions:
      - "text: buildExecApprovalRequestMessage(params.request, params.nowMs),"
      - "allowedDecisions: resolveExecApprovalRequestAllowedDecisions(params.request.request),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildExecViewBase
    file: infra/approval-view-model.ts
    line: 60
    actions:
      - "const { commandText, commandPreview } = resolveExecApprovalCommandDisplay(request.request);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildGenericNativeExecApprovalFallbackText
    file: infra/exec-approval-reply.ts
    line: 76
    actions:
      - "const clients = resolveNativeExecApprovalClientList({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildHostApprovalDecisionParams
    file: agents/bash-tools.exec-approval-request.ts
    line: 237
    actions:
      - "...buildExecApprovalRequesterContext({"
      - "...buildExecApprovalTurnSourceContext(params),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildLocalExecPolicyShowPayload
    file: cli/exec-policy-cli.ts
    line: 224
    actions:
      - "const approvalsSnapshot = readExecApprovalsSnapshot();"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildPendingApprovalView
    file: infra/approval-view-model.ts
    line: 104
    actions:
      - "actions: buildExecApprovalActionDescriptors({"
      - "actions: buildExecApprovalActionDescriptors({"
      - "allowedDecisions: resolveExecApprovalRequestAllowedDecisions(execRequest.request),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildTuiCliArgs
    file: tui/tui-launch.ts
    line: 61
    actions:
      - "const args = [...filterTuiExecArgv(process.execArgv), ...buildCurrentCliEntryArgs(), \"tui\"];"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupLegacyLaunchdService
    file: commands/doctor-gateway-services.ts
    line: 222
    actions:
      - "await execFileAsync(\"launchctl\", [\"bootout\", domain, params.plistPath]).catch(() => undefined);"
      - "await execFileAsync(\"launchctl\", [\"unload\", params.plistPath]).catch(() => undefined);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: clearActiveEmbeddedRun
    file: agents/pi-embedded-runner/runs.ts
    line: 389
    actions:
      - EMBEDDED_RUN_MODEL_SWITCH_REQUESTS.delete(sessionId);
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: clearInFlight
    file: logging/diagnostic-session-recovery-coordinator.ts
    line: 152
    actions:
      - recoveryRequestsInFlight.delete(inFlightKey);
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: collectExecPolicyConflictWarnings
    file: commands/doctor-security.ts
    line: 80
    actions:
      - "const approvals = loadExecApprovals();"
      - "askConfigured && execAskRank(snapshot.ask.requested) < execAskRank(snapshot.ask.effective);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: collectExecRuntimeFindings
    file: security/audit.ts
    line: 484
    actions:
      - "const approvals = loadExecApprovals();"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: collectPluginSecurityAuditFindings
    file: security/audit.ts
    line: 341
    actions:
      - requestedPluginIds.delete(pluginId);
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: consumeEmbeddedRunModelSwitch
    file: agents/pi-embedded-runner/runs.ts
    line: 236
    actions:
      - EMBEDDED_RUN_MODEL_SWITCH_REQUESTS.delete(normalizedSessionId);
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: copyExecApprovalsFallback
    file: infra/exec-approvals.ts
    line: 461
    actions:
      - "const destination = openExecApprovalsFallbackDestination(filePath);"
      - "writeExecApprovalsFallbackBuffer(destination.fd, contents);"
      - "validateExecApprovalsFallbackFd(filePath, destination.fd);"
      - "restoreExecApprovalsFallbackDestination(filePath, destination);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createAndRegisterDefaultExecApprovalRequest
    file: agents/bash-tools.exec-host-shared.ts
    line: 263
    actions:
      - "} = createDefaultExecApprovalRequestContext({"
      - "resolveExecApprovalUnavailableState({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createAnthropicMessagesClient
    file: agents/anthropic-transport-stream.ts
    line: 601
    actions:
      - "const response = await params.fetch(url, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: createChannelNativeApprovalRuntime
    file: infra/approval-native-runtime.ts
    line: 173
    actions:
      - "const runtime = createExecApprovalChannelRuntime<TPendingEntry, TRequest, TResolved>({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createDefaultExecApprovalRequestContext
    file: agents/bash-tools.exec-host-shared.ts
    line: 159
    actions:
      - "return createExecApprovalRequestContext({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createDefaultGatewayTestSpeechProviders
    file: gateway/test-helpers.speech.ts
    line: 60
    actions:
      - "const audioBuffer = await fetchStubSpeechAudio("
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: createDockerSandboxBackendHandle
    file: agents/sandbox/docker-backend.ts
    line: 49
    actions:
      - "...buildDockerExecArgs({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createExecApprovalAlertPayload
    file: infra/push-apns.ts
    line: 905
    actions:
      - "body: resolveExecApprovalAlertBody(),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createExecApprovalDecisionState
    file: agents/bash-tools.exec-host-shared.ts
    line: 333
    actions:
      - "const baseDecision = resolveBaseExecApprovalDecision({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createExecApprovalForwarder
    file: infra/exec-approval-forwarder.ts
    line: 772
    actions:
      - "const { sendDurableMessageBatch } = await loadExecApprovalForwarderRuntime();"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createExecApprovalRequestContext
    file: agents/bash-tools.exec-host-shared.ts
    line: 135
    actions:
      - "const pendingState = createExecApprovalRequestState({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createExecApprovalRequestState
    file: agents/bash-tools.exec-host-shared.ts
    line: 120
    actions:
      - "const pendingState = createExecApprovalPendingState({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createExecApprovalsFallbackDestination
    file: infra/exec-approvals.ts
    line: 386
    actions:
      - "validateExecApprovalsFallbackFd(filePath, fd);"
      - return openExistingExecApprovalsFallbackDestination(filePath);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: defaultResolveSessionTarget
    file: infra/exec-approval-forwarder.ts
    line: 331
    actions:
      - "return loadExecApprovalForwarderRuntime().then(({ resolveExecApprovalSessionTarget }) => {"
      - "const resolvedTarget = resolveExecApprovalSessionTarget({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: describeExecTool
    file: agents/bash-tools.descriptions.ts
    line: 16
    actions:
      - "const approvalsFile = loadExecApprovals();"
      - "const approvals = resolveExecApprovalsFromFile({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: describeNativeExecApprovalClientSetup
    file: infra/exec-approval-surface.ts
    line: 95
    actions:
      - "resolveChannelApprovalCapability(getChannelPlugin(channel))?.describeExecApprovalSetup?.({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: detectSystemTimeFormat
    file: agents/date-time.ts
    line: 96
    actions:
      - "const result = execFileSync(\"defaults\", [\"read\", \"-g\", \"AppleICUForce24HourTime\"], {"
      - "const result = execFileSync("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: ensureDebugProxyCa
    file: proxy-capture/ca.ts
    line: 9
    actions:
      - "await execFileAsync(openssl, ["
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: ensureDir
    file: infra/exec-approvals.ts
    line: 266
    actions:
      - "assertNoExecApprovalsSymlinkParents(dir, resolveRequiredHomeDir());"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: ensureExecApprovals
    file: infra/exec-approvals.ts
    line: 745
    actions:
      - "const loaded = loadExecApprovals();"
      - "const next = normalizeExecApprovals(loaded);"
      - "path: socketPath && socketPath.length > 0 ? socketPath : resolveExecApprovalsSocketPath(),"
      - saveExecApprovals(updated);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: evaluateExecAllowlist
    file: infra/exec-approvals-allowlist.ts
    line: 646
    actions:
      - "const allowlistContext = pickExecAllowlistContext(params);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: evaluateShellAllowlist
    file: infra/exec-approvals-allowlist.ts
    line: 1078
    actions:
      - "const allowlistContext = pickExecAllowlistContext(params);"
      - "const evaluation = evaluateExecAllowlist({ analysis, ...allowlistContext });"
      - "evaluation: evaluateExecAllowlist({ analysis, ...allowlistContext }),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: evaluateSystemRunAllowlist
    file: node-host/invoke-system-run-allowlist.ts
    line: 23
    actions:
      - "const allowlistEval = evaluateExecAllowlist({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: exchangeChutesCodeForTokens
    file: agents/chutes-oauth.ts
    line: 108
    actions:
      - "const response = await fetchFn(CHUTES_TOKEN_ENDPOINT, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: execConfigOpenCommand
    file: gateway/server-methods/config.ts
    line: 161
    actions:
      - "execFile(command.command, command.args, (error) => {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: execDockerRaw
    file: agents/sandbox/docker.ts
    line: 67
    actions:
      - "const child = spawn(spawnInvocation.command, spawnInvocation.args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: execFileUtf8
    file: daemon/exec-file.ts
    line: 5
    actions:
      - "execFile(command, args, { ...options, encoding: \"utf8\" }, (error, stdout, stderr) => {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: findGatewayPidsOnPortSync
    file: infra/restart-stale-pids.ts
    line: 314
    actions:
      - "const res = spawnSync(lsof, [\"-nP\", `-iTCP:${port}`, \"-sTCP:LISTEN\", \"-Fpc\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: forceClearEmbeddedPiRun
    file: agents/pi-embedded-runner/runs.ts
    line: 410
    actions:
      - EMBEDDED_RUN_MODEL_SWITCH_REQUESTS.delete(sessionId);
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: formatDirectExecApprovalFollowupText
    file: agents/bash-tools.exec-approval-followup.ts
    line: 85
    actions:
      - "const parsed = parseExecApprovalResultText(resultText);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: geminiAnalyzePdf
    file: agents/tools/pdf-native-providers.ts
    line: 121
    actions:
      - "const res = await fetch(url, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: generateCompletionCache
    file: commands/doctor-completion.ts
    line: 22
    actions:
      - "const result = spawnSync(process.execPath, [binPath, \"completion\", \"--write-state\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: generateSelfSignedCert
    file: infra/tls/gateway.ts
    line: 26
    actions:
      - "await execFileAsync(opensslBin, ["
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: handleIncomingData
    file: agents/pi-bundle-lsp-runtime.ts
    line: 177
    actions:
      - session.pendingRequests.delete(record.id);
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: hasApprovalTurnSourceRoute
    file: infra/approval-turn-source.ts
    line: 4
    actions:
      - "resolveExecApprovalInitiatingSurfaceState({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: hasDurableExecApproval
    file: infra/exec-approvals.ts
    line: 1017
    actions:
      - "hasExactCommandDurableExecApproval({"
      - "hasSegmentDurableExecApproval({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: hasExperimentalWarningSuppressed
    file: entry.respawn.ts
    line: 46
    actions:
      - "return execArgv.some((arg) => arg === EXPERIMENTAL_WARNING_FLAG || arg === \"--no-warnings\");"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: hasLaunchEvidence
    file: daemon/schtasks.ts
    line: 705
    actions:
      - "const processSnapshot = spawnSync("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: initSelfPresence
    file: infra/system-presence.ts
    line: 49
    actions:
      - "const modelIdentifier = (() => {"
      - "const res = spawnSync(\"sysctl\", [\"-n\", \"hw.model\"], {"
      - "const res = spawnSync(\"sw_vers\", [\"-productVersion\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: isClientEnabled
    file: plugin-sdk/approval-client-helpers.ts
    line: 104
    actions:
      - "return isChannelExecApprovalClientEnabledFromConfig({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: isContainerRunning
    file: cli/container-target.ts
    line: 66
    actions:
      - "const result = params.deps.spawnSync("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: isExecApprovalDecisionAllowed
    file: infra/exec-approvals.ts
    line: 1264
    actions:
      - "return resolveExecApprovalAllowedDecisions({ ask: params.ask }).includes(params.decision);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: isExecDeniedResultText
    file: agents/exec-approval-result.ts
    line: 68
    actions:
      - "return parseExecApprovalResultText(resultText).kind === \"denied\";"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: killChild
    file: process/exec.ts
    line: 339
    actions:
      - "spawn(\"taskkill\", [\"/PID\", String(child.pid), \"/T\", \"/F\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: killPortWithFuser
    file: cli/ports.ts
    line: 96
    actions:
      - "const stdout = execFileSync(\"fuser\", args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: launchctlGetenv
    file: commands/doctor-platform-notes.ts
    line: 38
    actions:
      - "const result = await execFileAsync(\"/bin/launchctl\", [\"getenv\", name], { encoding: \"utf8\" });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: launchFallbackTaskScript
    file: daemon/schtasks.ts
    line: 323
    actions:
      - "const child = spawn(executable, args, {"
      - "const child = spawn(\"cmd.exe\", [\"/d\", \"/c\", scriptPath], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: launchTuiCli
    file: tui/tui-launch.ts
    line: 80
    actions:
      - "const child = spawn(process.execPath, args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: listLocalTuiProcesses
    file: commands/doctor-whatsapp-responsiveness.ts
    line: 37
    actions:
      - "const ps = spawnSync(\"ps\", [\"-axo\", \"pid=,command=\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: listNativeExecApprovalClientLabels
    file: infra/exec-approval-surface.ts
    line: 83
    actions:
      - ".filter((plugin) => hasNativeExecApprovalCapability(plugin.id))"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: listPortListeners
    file: cli/ports.ts
    line: 162
    actions:
      - "const out = execFileSync(\"netstat\", [\"-ano\", \"-p\", \"TCP\"], { encoding: \"utf-8\" });"
      - "const out = execFileSync(lsof, [\"-nP\", `-iTCP:${port}`, \"-sTCP:LISTEN\", \"-FpFc\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: loadExecApprovalHandlers
    file: gateway/server-aux-handlers.ts
    line: 69
    actions:
      - "createExecApprovalHandlers(execApprovalManager, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: loadExecApprovalReply
    file: agents/pi-embedded-subscribe.handlers.tools.ts
    line: 70
    actions:
      - return execApprovalReplyModuleLoader.load();
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: loadExecApprovals
    file: infra/exec-approvals.ts
    line: 684
    actions:
      - "const filePath = resolveExecApprovalsPath();"
      - "return normalizeExecApprovals({ version: 1, agents: {} });"
      - "return normalizeExecApprovals({ version: 1, agents: {} });"
      - return normalizeExecApprovals(parsed);
      - "return normalizeExecApprovals({ version: 1, agents: {} });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: loadSnapshotLocal
    file: cli/exec-approvals-cli.ts
    line: 86
    actions:
      - "const snapshot = readExecApprovalsSnapshot();"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: locateWindowsRegExe
    file: infra/windows-install-roots.ts
    line: 99
    actions:
      - "for (const candidate of getWindowsRegExeCandidates()) {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: macosVersion
    file: infra/os-summary.ts
    line: 14
    actions:
      - "const res = spawnSync(\"sw_vers\", [\"-productVersion\"], { encoding: \"utf-8\" });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: macOSVersion
    file: infra/system-presence.ts
    line: 64
    actions:
      - "const res = spawnSync(\"sw_vers\", [\"-productVersion\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: matchAllowlist
    file: infra/exec-command-resolution.ts
    line: 342
    actions:
      - "? matchesExecAllowlistPattern(pattern, resolvedPath)"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: matchesExecutableBasenamePattern
    file: infra/exec-command-resolution.ts
    line: 322
    actions:
      - "return [...candidates].some((candidate) => matchesExecAllowlistPattern(pattern, candidate));"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: maybeRunCliInContainer
    file: cli/container-target.ts
    line: 256
    actions:
      - "const result = resolvedDeps.spawnSync("
      - "buildContainerExecArgs({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: maybeWarn
    file: commands/doctor-security.ts
    line: 86
    actions:
      - "askConfigured && execAskRank(snapshot.ask.requested) < execAskRank(snapshot.ask.effective);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: mergeExecApprovalsSocketDefaults
    file: infra/exec-approvals.ts
    line: 630
    actions:
      - "params.normalized.socket?.path?.trim() ?? currentSocketPath ?? resolveExecApprovalsSocketPath();"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: minimaxUnderstandImage
    file: agents/minimax-vlm.ts
    line: 48
    actions:
      - "const res = await fetch(url, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: normalizeAsk
    file: infra/exec-approvals.ts
    line: 773
    actions:
      - "return isExecAsk(value) ? value : fallback;"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: normalizeExecApprovals
    file: infra/exec-approvals.ts
    line: 585
    actions:
      - "const sanitizedPolicy = sanitizeExecApprovalPolicy(agent);"
      - "const sanitizedDefaults = sanitizeExecApprovalPolicy(file.defaults);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: normalizePreview
    file: infra/exec-approval-command-display.ts
    line: 180
    actions:
      - "const preview = sanitizeExecApprovalDisplayText(previewRaw);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: openExecApprovalsFallbackDestination
    file: infra/exec-approvals.ts
    line: 415
    actions:
      - return openExistingExecApprovalsFallbackDestination(filePath);
      - return createExecApprovalsFallbackDestination(filePath);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: openExistingExecApprovalsFallbackDestination
    file: infra/exec-approvals.ts
    line: 364
    actions:
      - "validateExecApprovalsFallbackFd(filePath, fd);"
      - "snapshot: readExecApprovalsFallbackSnapshotFromFd(fd),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: parseExecDirectiveArgs
    file: auto-reply/reply/exec/directive.ts
    line: 44
    actions:
      - "execAsk = normalizeExecAsk(value);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: pollPortOnce
    file: infra/restart-stale-pids.ts
    line: 372
    actions:
      - "const res = spawnSync(lsof, [\"-nP\", `-iTCP:${port}`, \"-sTCP:LISTEN\", \"-Fpc\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: postJsonRequest
    file: media-understanding/shared.ts
    line: 394
    actions:
      - return fetchWithTimeoutGuarded(
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: postMultipartRequest
    file: media-understanding/shared.ts
    line: 425
    actions:
      - return fetchWithTimeoutGuarded(
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: postTranscriptionRequest
    file: media-understanding/shared.ts
    line: 363
    actions:
      - return fetchWithTimeoutGuarded(
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: probeLocalCommand
    file: crestodian/probes.ts
    line: 10
    actions:
      - "const child = spawn(command, args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: probeZaiChatCompletions
    file: plugins/provider-zai-endpoint.ts
    line: 29
    actions:
      - "const res = await fetchWithTimeout("
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: pruneExpiredExecApprovalFollowupRuntimeHandoffs
    file: agents/bash-tools.exec-approval-followup-state.ts
    line: 57
    actions:
      - execApprovalFollowupRuntimeHandoffs.delete(handoffId);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: pruneSandboxBrowsers
    file: agents/sandbox/prune.ts
    line: 88
    actions:
      - "await dockerSandboxBackendManager.removeRuntime({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readCodexKeychainAuthRecord
    file: agents/cli-credentials.ts
    line: 248
    actions:
      - "const secret = execSyncImpl("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readDarwinCmdline
    file: infra/gateway-lock.ts
    line: 112
    actions:
      - "const raw = execFileSync(\"ps\", [\"-p\", String(pid), \"-o\", \"command=\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readExecApprovalsSnapshot
    file: infra/exec-approvals.ts
    line: 652
    actions:
      - "const filePath = resolveExecApprovalsPath();"
      - "const file = normalizeExecApprovals({ version: 1, agents: {} });"
      - "hash: hashExecApprovalsRaw(null),"
      - "? normalizeExecApprovals(parsed)"
      - ": normalizeExecApprovals({ version: 1, agents: {} });"
      - "hash: hashExecApprovalsRaw(raw),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readGatewayProcessArgsSync
    file: infra/gateway-processes.ts
    line: 10
    actions:
      - "const ps = spawnSync(\"ps\", [\"-o\", \"command=\", \"-p\", String(pid)], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readListeningPidsViaPowerShell
    file: infra/windows-port-pids.ts
    line: 19
    actions:
      - "const ps = spawnSync("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readUnixProcessArgsSync
    file: infra/restart-stale-pids.ts
    line: 211
    actions:
      - "const res = spawnSync(\"ps\", [\"-ww\", \"-p\", String(pid), \"-o\", \"command=\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readUserCrontab
    file: commands/doctor-cron.ts
    line: 145
    actions:
      - "const result = await execFileAsync(\"crontab\", [\"-l\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readWindowsCmdline
    file: infra/gateway-lock.ts
    line: 81
    actions:
      - "const buf = execFileSync("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readWindowsListeningPidsResultSync
    file: infra/windows-port-pids.ts
    line: 66
    actions:
      - "const netstat = spawnSync(\"netstat\", [\"-ano\", \"-p\", \"tcp\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readWindowsProcessArgsResultSync
    file: infra/windows-port-pids.ts
    line: 116
    actions:
      - "const powershell = spawnSync("
      - "const wmic = spawnSync("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: recordAllowlistUse
    file: infra/exec-approvals.ts
    line: 1068
    actions:
      - saveExecApprovals(approvals);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: refreshChutesTokens
    file: agents/chutes-oauth.ts
    line: 168
    actions:
      - "const response = await fetchFn(CHUTES_TOKEN_ENDPOINT, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: registerExecApprovalRequest
    file: agents/bash-tools.exec-approval-request.ts
    line: 109
    actions:
      - "buildExecApprovalRequestToolParams(params),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: registerExecApprovalRequestForHost
    file: agents/bash-tools.exec-approval-request.ts
    line: 271
    actions:
      - return await registerExecApprovalRequest(await buildHostApprovalDecisionParams(params));
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: registerExecApprovalRequestForHostOrThrow
    file: agents/bash-tools.exec-approval-request.ts
    line: 277
    actions:
      - return await registerExecApprovalRequestForHost(params);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: registerGatewayApproval
    file: agents/bash-tools.exec-host-gateway.ts
    line: 367
    actions:
      - "await registerExecApprovalRequestForHostOrThrow({"
      - "...buildExecApprovalRequesterContext({"
      - "...buildExecApprovalTurnSourceContext(params),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: registerNodeApproval
    file: agents/bash-tools.exec-host-node.ts
    line: 85
    actions:
      - "await registerExecApprovalRequestForHostOrThrow({"
      - "...buildExecApprovalRequesterContext({"
      - "...buildExecApprovalTurnSourceContext(params),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removeActiveProviderIds
    file: plugins/capability-provider-runtime.ts
    line: 351
    actions:
      - requested.delete(provider.id.toLowerCase());
      - requested.delete(alias.toLowerCase());
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: removeSandboxBrowserContainer
    file: agents/sandbox/manage.ts
    line: 106
    actions:
      - "await dockerSandboxBackendManager.removeRuntime({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: requestExecApprovalDecision
    file: agents/bash-tools.exec-approval-request.ts
    line: 158
    actions:
      - "const registration = await registerExecApprovalRequest(params);"
      - return await waitForExecApprovalDecision(registration.id);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: requestExecApprovalDecisionForHost
    file: agents/bash-tools.exec-approval-request.ts
    line: 265
    actions:
      - return await requestExecApprovalDecision(await buildHostApprovalDecisionParams(params));
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: requestStuckSessionRecovery
    file: logging/diagnostic-session-recovery-coordinator.ts
    line: 125
    actions:
      - recoveryRequestsInFlight.delete(inFlightKey);
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: requestVerification
    file: commands/onboard-custom.ts
    line: 91
    actions:
      - "const res = await fetchWithTimeout("
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: resetExecApprovalFollowupRuntimeHandoffsForTests
    file: agents/bash-tools.exec-approval-followup-state.ts
    line: 148
    actions:
      - execApprovalFollowupRuntimeHandoffs.clear();
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveAgentAskField
    file: infra/exec-approvals.ts
    line: 854
    actions:
      - "if (isExecAsk(params.agent.ask)) {"
      - "if (isExecAsk(params.wildcard.ask)) {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveAllowedDecisions
    file: infra/exec-approval-reply.ts
    line: 85
    actions:
      - "return params.allowedDecisions ?? resolveExecApprovalAllowedDecisions({ ask: params.ask });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveApprovalDecisionOrUndefined
    file: agents/bash-tools.exec-host-shared.ts
    line: 216
    actions:
      - "return await resolveRegisteredExecApprovalDecision({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveApprovalRequestSessionTarget
    file: infra/exec-approval-session-target.ts
    line: 152
    actions:
      - "return resolveExecApprovalSessionTarget({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveApprovalRequestStoredSessionTarget
    file: infra/exec-approval-session-target.ts
    line: 167
    actions:
      - "return resolveExecApprovalSessionTarget({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveBinaryPath
    file: daemon/program-args.ts
    line: 163
    actions:
      - "const output = execFileSync(cmd, [binary], { encoding: \"utf8\" }).trim();"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveCodexCliBin
    file: tui/tui.ts
    line: 88
    actions:
      - "const raw = execFileSync(lookupCmd, [\"codex\"], { encoding: \"utf8\" }).trim();"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveCommandSpans
    file: agents/bash-tools.exec-approval-request.ts
    line: 223
    actions:
      - "const { resolveExecApprovalCommandSpans } = await loadExecApprovalCommandSpansRuntime();"
      - return await resolveExecApprovalCommandSpans(command);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveDefaultAskField
    file: infra/exec-approvals.ts
    line: 800
    actions:
      - "if (isExecAsk(params.defaults.ask)) {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveExecApprovalAllowedDecisions
    file: infra/exec-approvals.ts
    line: 1241
    actions:
      - "const ask = normalizeExecAsk(params?.ask);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveExecApprovalCommandDisplay
    file: infra/exec-approval-command-display.ts
    line: 192
    actions:
      - "const commandText = sanitizeExecApprovalDisplayText(commandTextSource);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveExecApprovalRequestAllowedDecisions
    file: infra/exec-approvals.ts
    line: 1251
    actions:
      - "return explicit.length > 0 ? explicit : resolveExecApprovalAllowedDecisions({ ask: params?.ask });"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveExecApprovals
    file: infra/exec-approvals.ts
    line: 895
    actions:
      - "const file = ensureExecApprovals();"
      - "return resolveExecApprovalsFromFile({"
      - "path: resolveExecApprovalsPath(),"
      - "socketPath: expandHomePrefix(file.socket?.path ?? resolveExecApprovalsSocketPath()),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveExecApprovalsFromFile
    file: infra/exec-approvals.ts
    line: 910
    actions:
      - "const file = normalizeExecApprovals(params.file);"
      - "path: params.path ?? resolveExecApprovalsPath(),"
      - "params.socketPath ?? file.socket?.path ?? resolveExecApprovalsSocketPath(),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveExecApprovalUnavailableState
    file: agents/bash-tools.exec-host-shared.ts
    line: 232
    actions:
      - "const initiatingSurface = resolveExecApprovalInitiatingSurfaceState({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveExecDefaults
    file: agents/exec-defaults.ts
    line: 91
    actions:
      - "const approvalDefaults = loadExecApprovals().defaults;"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveExecHostApprovalContext
    file: agents/bash-tools.exec-host-shared.ts
    line: 195
    actions:
      - "const approvals = resolveExecApprovals(params.agentId, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveExecPolicyInput
    file: cli/exec-policy-cli.ts
    line: 135
    actions:
      - "const ask = normalizeExecAsk(params.ask);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveExecPolicyScopeSnapshot
    file: infra/exec-approvals-effective.ts
    line: 206
    actions:
      - "const resolved = resolveExecApprovalsFromFile({"
      - "allowedDecisions: resolveExecApprovalAllowedDecisions({ ask: effectiveAsk }),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveGatewayDecisionFromPermissionOutcome
    file: acp/permission-relay.ts
    line: 156
    actions:
      - "return normalizeGatewayExecApprovalDecision(selected?.optionId);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveNativeExecApprovalClientList
    file: infra/exec-approval-reply.ts
    line: 68
    actions:
      - "listNativeExecApprovalClientLabels({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveNodeVersion
    file: daemon/runtime-paths.ts
    line: 80
    actions:
      - "const { stdout } = await execFileImpl(nodePath, [\"-p\", \"process.versions.node\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolvePluginConversationBindingApproval
    file: plugins/conversation-binding.ts
    line: 881
    actions:
      - pendingRequests.delete(params.approvalId);
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: resolveRegisteredExecApprovalDecision
    file: agents/bash-tools.exec-approval-request.ts
    line: 148
    actions:
      - return await waitForExecApprovalDecision(params.approvalId);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveSshConfig
    file: infra/ssh-config.ts
    line: 57
    actions:
      - "const child = spawn(sshPath, args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveWindowsConsoleEncoding
    file: infra/windows-encoding.ts
    line: 31
    actions:
      - "const result = spawnSync(\"cmd.exe\", [\"/d\", \"/s\", \"/c\", \"chcp\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: run
    file: cli/dns-cli.ts
    line: 15
    actions:
      - "const res = spawnSync(cmd, args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runCodeModeChild
    file: agents/tool-search.ts
    line: 1171
    actions:
      - "const child = spawn(process.execPath, buildCodeModeChildArgs(), {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runCommand
    file: node-host/invoke.ts
    line: 137
    actions:
      - "const child = spawn(argv[0], argv.slice(1), {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runCommandWithTimeout
    file: process/exec.ts
    line: 281
    actions:
      - "const child = spawn(invocation.command, invocation.args, {"
      - "spawn(\"taskkill\", [\"/PID\", String(child.pid), \"/T\", \"/F\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runDebugProxyRunCommand
    file: cli/proxy-cli.runtime.ts
    line: 68
    actions:
      - "const child = spawn(command, args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runDirectImportSmoke
    file: plugin-sdk/test-helpers/direct-smoke.ts
    line: 17
    actions:
      - "const { stdout } = await execFileAsync(process.execPath, [\"--import\", \"tsx\", \"-e\", code], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runExec
    file: process/exec.ts
    line: 143
    actions:
      - "const { stdout, stderr } = (await execFileAsync(invocation.command, invocation.args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runExecProcess
    file: agents/bash-tools.exec-runtime.ts
    line: 554
    actions:
      - "} = await (async () => {"
      - "...buildDockerExecArgs({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runFfmpeg
    file: media/ffmpeg-exec.ts
    line: 75
    actions:
      - "const { stdout } = await execFileAsync("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runFfprobe
    file: media/ffmpeg-exec.ts
    line: 48
    actions:
      - "const { stdout } = await execFileAsync(requireSystemBin(\"ffprobe\"), args, execOptions);"
      - "const proc = execFile(requireSystemBin(\"ffprobe\"), args, execOptions, (err, stdout) => {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runLocalShellLine
    file: tui/tui-local-shell.ts
    line: 81
    actions:
      - "const allowed = await ensureLocalExecAllowed();"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runOpenClawCliJson
    file: gateway/live-agent-probes.ts
    line: 125
    actions:
      - "const { stdout, stderr } = await execFileAsync(process.execPath, [\"openclaw.mjs\", ...args], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runRegQuery
    file: infra/windows-install-roots.ts
    line: 118
    actions:
      - "return execFileSync(regExe, args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runRestartScript
    file: cli/update-cli/restart-helper.ts
    line: 393
    actions:
      - "const child = spawn(file, args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runSshSandboxCommand
    file: agents/sandbox/ssh.ts
    line: 209
    actions:
      - "const child = spawn(argv[0], argv.slice(1), {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runTaskkill
    file: process/kill-tree.ts
    line: 100
    actions:
      - "spawn(\"taskkill\", args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: sanitizeExecApprovalDisplayText
    file: infra/exec-approval-command-display.ts
    line: 169
    actions:
      - return sanitizeExecApprovalDisplayTextInternal(commandText);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: sanitizeExecApprovalWarningText
    file: infra/exec-approval-command-display.ts
    line: 173
    actions:
      - "return sanitizeExecApprovalDisplayTextInternal(normalizeDisplayLineBreaks(warningText), {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: sanitizeExecPolicyTableCell
    file: cli/exec-policy-cli.ts
    line: 122
    actions:
      - return sanitizeExecApprovalDisplayText(sanitizeTerminalText(value));
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: saveExecApprovals
    file: infra/exec-approvals.ts
    line: 701
    actions:
      - "const filePath = resolveExecApprovalsPath();"
      - "writeExecApprovalsRaw(filePath, raw);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: saveSnapshotLocal
    file: cli/exec-approvals-cli.ts
    line: 96
    actions:
      - saveExecApprovals(file);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: scheduleDetachedLaunchdRestartHandoff
    file: daemon/launchd-restart-handoff.ts
    line: 188
    actions:
      - "const child = spawn("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: scpFile
    file: auto-reply/reply/stage-sandbox-media.ts
    line: 311
    actions:
      - "const child = spawn("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: sendApnsExecApprovalAlert
    file: infra/push-apns.ts
    line: 1095
    actions:
      - "const payload = createExecApprovalAlertPayload({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: sendApnsExecApprovalResolvedWake
    file: infra/push-apns.ts
    line: 1127
    actions:
      - "const payload = createExecApprovalResolvedPayload({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: sendApnsRelayRequest
    file: infra/push-apns.relay.ts
    line: 168
    actions:
      - "const response = await fetch(`${params.relayConfig.baseUrl}/v1/push/send`, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: sendRequest
    file: agents/pi-bundle-lsp-runtime.ts
    line: 160
    actions:
      - session.pendingRequests.delete(id);
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: sendRequestedPushes
    file: gateway/exec-approval-ios-push.ts
    line: 177
    actions:
      - "? await sendApnsExecApprovalAlert({"
      - ": await sendApnsExecApprovalAlert({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: sendResolvedPushes
    file: gateway/exec-approval-ios-push.ts
    line: 223
    actions:
      - "? await sendApnsExecApprovalResolvedWake({"
      - ": await sendApnsExecApprovalResolvedWake({"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: shouldFallbackScheduledTaskLaunch
    file: daemon/schtasks.ts
    line: 671
    actions:
      - "const processSnapshot = spawnSync("
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: shouldPrefixDirectFollowupWithSessionResumeFailure
    file: agents/bash-tools.exec-approval-followup.ts
    line: 127
    actions:
      - "const parsed = parseExecApprovalResultText(params.resultText);"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: shouldSuppressLocalPrompt
    file: plugin-sdk/approval-client-helpers.ts
    line: 154
    actions:
      - "return getExecApprovalReplyMetadata(input.payload) !== null;"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: spawnDetachedGatewayProcess
    file: infra/process-respawn.ts
    line: 25
    actions:
      - "const child = spawn(process.execPath, args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: spawnGogServe
    file: hooks/gmail-ops.ts
    line: 394
    actions:
      - "return spawn(invocation.command, invocation.args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: spawnGogServe
    file: hooks/gmail-watcher.ts
    line: 98
    actions:
      - "const child = spawn(invocation.command, invocation.args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: spawnLspServerProcess
    file: agents/pi-bundle-lsp-runtime.ts
    line: 72
    actions:
      - "return spawn(invocation.command, invocation.argv, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: start
    file: agents/mcp-stdio-transport.ts
    line: 42
    actions:
      - "const child = spawn(preparedSpawn.command, preparedSpawn.args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: startSshPortForward
    file: infra/ssh-tunnel.ts
    line: 103
    actions:
      - "const child = spawn(\"/usr/bin/ssh\", args, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: stopPostCoreUpdateChild
    file: cli/update-cli/update-command.ts
    line: 1814
    actions:
      - "const killer = spawn(\"taskkill\", [\"/PID\", String(child.pid), \"/T\", \"/F\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: supportsNativeExecApprovalClient
    file: infra/exec-approval-surface.ts
    line: 75
    actions:
      - return hasNativeExecApprovalCapability(normalized);
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: synthesizeElevenLabsLiveSpeech
    file: plugin-sdk/test-helpers/stt-live-audio.ts
    line: 38
    actions:
      - "const response = await fetch(url, {"
    checks: []
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: terminateGatewayProcessTree
    file: daemon/schtasks.ts
    line: 488
    actions:
      - "spawnSync(taskkillPath, [\"/T\", \"/PID\", String(pid)], {"
      - "spawnSync(taskkillPath, [\"/F\", \"/T\", \"/PID\", String(pid)], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: terminateStaleProcessesWindows
    file: infra/restart-stale-pids.ts
    line: 474
    actions:
      - "const graceful = spawnSync(taskkillPath, [\"/T\", \"/PID\", String(pid)], {"
      - "const forced = spawnSync(taskkillPath, [\"/F\", \"/T\", \"/PID\", String(pid)], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: toExecApprovalsPayload
    file: gateway/server-methods/exec-approvals.ts
    line: 80
    actions:
      - "file: redactExecApprovals(snapshot.file),"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: triggerOpenClawRestart
    file: infra/restart.ts
    line: 563
    actions:
      - "const userRestart = spawnSync(\"systemctl\", userArgs, {"
      - "const systemRestart = spawnSync(\"systemctl\", systemArgs, {"
      - "const res = spawnSync(\"launchctl\", args, {"
      - "const boot = spawnSync(\"launchctl\", bootstrapArgs, {"
      - "const retry = spawnSync(\"launchctl\", retryArgs, {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: tryScutil
    file: infra/machine-name.ts
    line: 10
    actions:
      - "const { stdout } = await execFileAsync(\"/usr/sbin/scutil\", [\"--get\", key], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: tryWriteCompletionCache
    file: cli/update-cli/shared.ts
    line: 266
    actions:
      - "const result = spawnSync(resolveNodeRunner(), [binPath, \"completion\", \"--write-state\"], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: uploadDirectoryToSshTarget
    file: agents/sandbox/ssh.ts
    line: 255
    actions:
      - "const tar = spawn(\"tar\", [\"-C\", params.localDir, \"-cf\", \"-\", \".\"], {"
      - "const ssh = spawn(sshArgv[0], sshArgv.slice(1), {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: writeClaudeCliKeychainCredentials
    file: agents/cli-credentials.ts
    line: 482
    actions:
      - "const existingResult = execFileSyncImpl("
      - execFileSyncImpl(
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: writeFileSudoIfNeeded
    file: cli/dns-cli.ts
    line: 33
    actions:
      - "const res = spawnSync(\"sudo\", [\"tee\", filePath], {"
    checks: []
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]


  # ⚠ NO CHECKS — publish
  - function: buildAudioTranscriptionFormData
    file: media-understanding/shared.ts
    line: 42
    actions:
      - "form.append(\"file\", blob, resolveAudioTranscriptionUploadFileName(params.fileName, params.mime));"
    checks: []
    missing:
      - no auth check
      - no input validation
    owasp: [ASI-01, ASI-02]


  # ~ PARTIAL CHECKS
  - function: analyzeNodeApprovalRequirement
    file: agents/bash-tools.exec-host-node-phases.ts
    line: 294
    actions:
      - "const resolved = resolveExecApprovalsFromFile({"
      - "durableApprovalSatisfied = hasDurableExecApproval({"
    checks:
      - type: approval_step
        code: "if (approvalsFile && typeof approvalsFile === \"object\")"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: applyLocalExecPolicy
    file: cli/exec-policy-cli.ts
    line: 337
    actions:
      - "const approvalsSnapshot = readExecApprovalsSnapshot();"
      - "const writtenApprovalsHash = hashExecApprovalsFile(nextApprovals);"
      - saveExecApprovals(nextApprovals);
      - "const currentApprovalsSnapshot = readExecApprovalsSnapshot();"
      - restoreExecApprovalsSnapshot(approvalsSnapshot);
    checks:
      - type: approval_step
        code: "if (currentApprovalsSnapshot.hash !== writtenApprovalsHash)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildAgentSystemPrompt
    file: agents/system-prompt.ts
    line: 634
    actions:
      - "buildExecApprovalPromptGuidance({"
    checks:
      - type: input_validation
        code: "if (!normalized || !value?.trim())"
      - type: input_validation
        code: "if (!isMinimal && silentReplyPromptMode !== \"none\")"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildApproverRestrictedNativeApprovalCapability
    file: plugin-sdk/approval-delivery-helpers.ts
    line: 61
    actions:
      - ": params.isExecAuthorizedSender({ cfg, accountId, senderId });"
    checks:
      - type: auth_check
        code: "? pluginSenderAuth({ cfg, accountId, senderId })"
      - type: auth_check
        code: ": params.isExecAuthorizedSender({ cfg, accountId, senderId });"
      - type: input_validation
        code: "if (!hasConfiguredApprovers({ cfg, accountId }))"
      - type: input_validation
        code: "if (!params.isNativeDeliveryEnabled({ cfg, accountId }))"
    missing: []
    owasp: [ASI-02, ASI-03]

  - function: buildCliRespawnPlan
    file: entry.respawn.ts
    line: 61
    actions:
      - childExecArgv.unshift(EXPERIMENTAL_WARNING_FLAG);
    checks:
      - type: input_validation
        code: "if (shouldSkipStartupEnvironmentRespawnForArgv(argv) ||
    isTruthyEnvValue(env.OPE)"
      - type: input_validation
        code: "if (autoNodeExtraCaCerts &&
    !isTruthyEnvValue(env[OPENCLAW_NODE_EXTRA_CA_CERTS_R)"
      - type: input_validation
        code: "if (!shouldSkipRespawnForArgv(argv) &&
    !isTruthyEnvValue(env[OPENCLAW_NODE_OPTIO)"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildExecApprovalActionDescriptors
    file: infra/exec-approval-reply.ts
    line: 108
    actions:
      - "command: buildExecApprovalCommandText({"
      - "command: buildExecApprovalCommandText({"
      - "command: buildExecApprovalCommandText({"
    checks:
      - type: approval_step
        code: "if (!approvalCommandId)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: buildTestOpenAISpeechProvider
    file: plugins/contracts/tts-contract-suites.ts
    line: 222
    actions:
      - "await fetch(`${resolveBaseUrl(config?.baseUrl, \"https://api.openai.com/v1\")}/audio/speech`, {"
      - "await fetch(`${resolveBaseUrl(config?.baseUrl, \"https://api.openai.com/v1\")}/audio/speech`, {"
    checks:
      - type: input_validation
        code: "if (isDefaultEndpoint && !allowedVoices.has(value))"
      - type: input_validation
        code: "if (isDefaultEndpoint && !allowedModels.has(value))"
    missing:
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: callLoopbackJsonRpc
    file: gateway/gateway-cli-backend.live-probe-helpers.ts
    line: 169
    actions:
      - "const response = await fetch(`http://127.0.0.1:${runtime.port}/mcp`, {"
    checks:
      - type: input_validation
        code: if (params.accountId)
    missing:
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: captureCompactionCheckpointSnapshotAsync
    file: gateway/session-compaction-checkpoints.ts
    line: 300
    actions:
      - await fs.unlink(snapshotFile);
    checks:
      - type: input_validation
        code: "if (!stat.isFile() || stat.size > maxBytes)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: cleanupGatewayTestHome
    file: gateway/test-helpers.server.ts
    line: 367
    actions:
      - "await fs.rm(tempHome, {"
    checks:
      - type: retry_bound
        code: "await fs.rm(tempHome, {"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: collectPositionalTokens
    file: infra/exec-safe-bin-policy-validator.ts
    line: 137
    actions:
      - "const token = parseExecArgvToken(rawToken);"
    checks:
      - type: input_validation
        code: "if (token.kind === \"terminator\")"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: consumeExecApprovalFollowupRuntimeHandoff
    file: agents/bash-tools.exec-approval-followup-state.ts
    line: 113
    actions:
      - pruneExpiredExecApprovalFollowupRuntimeHandoffs(nowMs);
      - execApprovalFollowupRuntimeHandoffs.delete(handoffId);
      - execApprovalFollowupRuntimeHandoffs.delete(handoffId);
      - return cloneExecApprovalFollowupRuntimeHandoff(entry);
    checks:
      - type: approval_step
        code: "if (!handoffId || !approvalId || !idempotencyKey)"
      - type: approval_step
        code: "if (entry.approvalId !== approvalId ||
    entry.idempotencyKey !== idempotencyKey |)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createAcpClient
    file: acp/client.ts
    line: 107
    actions:
      - "const agent = spawn(spawnInvocation.command, spawnInvocation.args, {"
    checks:
      - type: auth_check
        code: "const stripProviderAuthEnvVars = shouldStripProviderAuthEnvVarsForAcpServer({"
    missing:
      - no input validation
    owasp: [ASI-02, ASI-03]

  - function: createApprovalRuntime
    file: gateway/node-invoke-plugin-policy.ts
    line: 45
    actions:
      - "params.context.hasExecApprovalClients?.(params.client?.connId) ?? false;"
    checks:
      - type: approval_step
        code: "if (!hasApprovalClients)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createBackupArchive
    file: infra/backup-create.ts
    line: 433
    actions:
      - "await fs.rm(tempArchivePath, { force: true }).catch(() => undefined);"
      - "await fs.rm(tempDir, { recursive: true, force: true }).catch(() => undefined);"
    checks:
      - type: input_validation
        code: "if (skippedVolatileCount > 0)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createChannelExecApprovalProfile
    file: plugin-sdk/approval-client-helpers.ts
    line: 92
    actions:
      - "return isChannelExecApprovalClientEnabledFromConfig({"
      - "!isChannelExecApprovalClientEnabledFromConfig({"
      - "return getExecApprovalReplyMetadata(input.payload) !== null;"
    checks:
      - type: input_validation
        code: "if (params.matchesRequestAccount && !params.matchesRequestAccount(input))"
      - type: input_validation
        code: "if (!isChannelExecApprovalClientEnabledFromConfig({
        enabled: config?.enabled)"
      - type: approval_step
        code: "if (!isChannelExecApprovalClientEnabledFromConfig({
        enabled: config?.enabled)"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createConfigIO
    file: config/io.ts
    line: 1234
    actions:
      - deps.fs.unlinkSync(filePath);
      - deps.fs.unlinkSync(migration.filePath);
    checks:
      - type: auth_check
        code: "const tildeRestoredOutputConfig = restoreAuthoredTildePathsForWrite("
    missing:
      - no confirmation step
    owasp: [ASI-02, ASI-03]

  - function: createExecApprovalChannelRuntime
    file: infra/exec-approval-channel-runtime.ts
    line: 76
    actions:
      - "startPromise = (async () => {"
    checks:
      - type: auth_check
        code: "startPromise = (async () => {"
      - type: approval_step
        code: "if (evt.event === \"exec.approval.requested\" && eventKinds.has(\"exec\"))"
      - type: approval_step
        code: "if (evt.event === \"plugin.approval.requested\" && eventKinds.has(\"plugin\"))"
      - type: approval_step
        code: "if (evt.event === \"exec.approval.resolved\" && eventKinds.has(\"exec\"))"
      - type: approval_step
        code: "if (evt.event === \"plugin.approval.resolved\" && eventKinds.has(\"plugin\"))"
    missing:
      - no input validation
    owasp: [ASI-02, ASI-03]

  - function: createExecApprovalHandlers
    file: gateway/server-methods/exec-approval.ts
    line: 81
    actions:
      - "if (!validateExecApprovalGetParams(params)) {"
      - "const { commandText, commandPreview } = resolveExecApprovalCommandDisplay("
      - "allowedDecisions: resolveExecApprovalRequestAllowedDecisions(resolved.snapshot.request),"
      - "if (!validateExecApprovalRequestParams(params)) {"
      - "const sanitizedCommandText = sanitizeExecApprovalDisplayText(effectiveCommandText);"
      - ": sanitizeExecApprovalDisplayText(approvalContext.commandPreview),"
      - "warningText: warningText ? sanitizeExecApprovalWarningText(warningText) : null,"
      - "allowedDecisions: resolveExecApprovalAllowedDecisions({ ask: p.ask ?? null }),"
      - "if (!validateExecApprovalResolveParams(params)) {"
      - "const allowedDecisions = resolveExecApprovalRequestAllowedDecisions(snapshot.request);"
    checks:
      - type: approval_step
        code: "if (!validateExecApprovalGetParams(params))"
      - type: approval_step
        code: "if (!validateExecApprovalRequestParams(params))"
      - type: approval_step
        code: "if (host === \"node\" && !approvalContext.plan)"
      - type: approval_step
        code: "if (explicitId?.startsWith(RESERVED_PLUGIN_APPROVAL_ID_PREFIX))"
      - type: approval_step
        code: "if (!validateExecApprovalResolveParams(params))"
      - type: approval_step
        code: "if (!isApprovalDecision(p.decision))"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createExecApprovalIosPushDelivery
    file: gateway/exec-approval-ios-push.ts
    line: 258
    actions:
      - "const deliveryStatePromise = (async (): Promise<ApprovalDeliveryState | null> => {"
    checks:
      - type: approval_step
        code: "const deliveryStatePromise = (async (): Promise<ApprovalDeliveryState | null> => {"
      - type: approval_step
        code: "if (approvalDeliveriesById.get(request.id)?.requestPushPromise ===
          deliver)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createExecTool
    file: agents/bash-tools.exec.ts
    line: 1170
    actions:
      - "const approvalDefaults = loadExecApprovals().defaults;"
      - "const requestedAsk = normalizeExecAsk(params.ask);"
    checks:
      - type: approval_step
        code: if (approvalWarningText)
      - type: approval_step
        code: if (bypassApprovals)
      - type: approval_step
        code: "if (host === \"gateway\" && !bypassApprovals)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createGatewayAuxHandlers
    file: gateway/server-aux-handlers.ts
    line: 54
    actions:
      - "const execApprovalForwarder = createExecApprovalForwarder();"
      - "const execApprovalIosPushDelivery = createExecApprovalIosPushDelivery({ log: params.log });"
      - "createExecApprovalHandlers(execApprovalManager, {"
    checks:
      - type: auth_check
        code: "disconnectStaleSharedGatewayAuthClients({"
      - type: auth_check
        code: "disconnectStaleSharedGatewayAuthClients({"
      - type: input_validation
        code: "if (isTruthyEnvValue(process.env.OPENCLAW_SKIP_CHANNELS) ||
                    isTr)"
    missing: []
    owasp: [ASI-02, ASI-03]

  - function: createGooglePromptCache
    file: agents/pi-embedded-runner/google-prompt-cache.ts
    line: 218
    actions:
      - "const response = await params.fetchImpl(`${params.baseUrl}/cachedContents`, {"
    checks:
      - type: auth_check
        code: "headers: mergeTransportHeaders(parseGeminiAuth(params.apiKey).headers, params.headers),"
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: createLocalShellRunner
    file: tui/tui-local-shell.ts
    line: 27
    actions:
      - "const allowed = await ensureLocalExecAllowed();"
    checks:
      - type: input_validation
        code: "if (item.value === \"yes\")"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createManagedOutgoingImageBlocks
    file: gateway/managed-image-attachments.ts
    line: 770
    actions:
      - "await fs.rm(savedOriginal.path, { force: true }).catch(() => {});"
      - "await fs.rm(savedOriginal.path, { force: true }).catch(() => {});"
      - "await fs.rm(savedOriginalPath, { force: true }).catch(() => {});"
    checks:
      - type: input_validation
        code: "if (savedOriginal.size > limits.maxBytes)"
      - type: input_validation
        code: "if (originalStats.sizeBytes != null && originalStats.sizeBytes > limits.maxBytes)"
      - type: input_validation
        code: "if (!metadataLimitError)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createOpenClawTestState
    file: test-utils/openclaw-test-state.ts
    line: 246
    actions:
      - "await fs.rm(root, { recursive: true, force: true });"
    checks:
      - type: input_validation
        code: "if (value === undefined)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: createPluginRegistry
    file: plugins/registry.ts
    line: 319
    actions:
      - "run: (params) => withPluginRuntimePluginIdScope(pluginId, () => subagent.run(params)),"
    checks:
      - type: input_validation
        code: "if (!value)"
      - type: input_validation
        code: if (normalizedScope.coercedToReservedAdmin)
      - type: input_validation
        code: "if (value === undefined)"
      - type: input_validation
        code: "if (typeof value !== \"string\")"
      - type: input_validation
        code: "if (value === undefined)"
      - type: input_validation
        code: "if (!Array.isArray(value))"
      - type: input_validation
        code: "if (descriptor.schema !== undefined && !isPluginJsonValue(descriptor.schema))"
    missing:
      - no auth check
      - no retry bound
    owasp: [ASI-01, ASI-02, ASI-04, ASI-06, ASI-10]

  - function: createSuiteTempRootTracker
    file: test-helpers/temp-dir.ts
    line: 134
    actions:
      - "await fs.rm(currentRoot, {"
    checks:
      - type: retry_bound
        code: "await fs.rm(currentRoot, {"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: describeZAIProviderRuntimeContract
    file: plugin-sdk/test-helpers/provider-runtime-contract.ts
    line: 751
    actions:
      - "await fs.rm(home, { recursive: true, force: true });"
    checks:
      - type: auth_check
        code: "provider.resolveUsageAuth?.({"
      - type: auth_check
        code: "provider.resolveUsageAuth?.({"
      - type: input_validation
        code: "if (url.includes(\"api.z.ai/api/monitor/usage/quota/limit\"))"
    missing:
      - no confirmation step
    owasp: [ASI-02, ASI-03]

  - function: dispatchReplyFromConfig
    file: auto-reply/reply/dispatch-from-config.ts
    line: 364
    actions:
      - "shouldSuppressLocalExecApprovalPrompt({"
    checks:
      - type: input_validation
        code: "if (!shouldEmitVerboseProgress() ||
        !shouldSendToolStartStatuses ||
        )"
      - type: input_validation
        code: "if (shouldSuppressLocalExecApprovalPrompt({
          channel: normalizeMessageChann)"
      - type: approval_step
        code: "if (shouldSuppressLocalExecApprovalPrompt({
          channel: normalizeMessageChann)"
      - type: approval_step
        code: "if (execApproval && typeof execApproval === \"object\" && !Array.isArray(execApproval))"
      - type: approval_step
        code: "if (!hasMedia && !hasExecApproval && deliveryPayload.isError !== true)"
      - type: input_validation
        code: "if (!finalReply.queuedFinal && finalReply.routedFinalCount === 0)"
      - type: input_validation
        code: "if (ttsMode === \"final\" &&
        replies.length === 0 &&
        blockCount > 0 &&)"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: downloadToFile
    file: media/store.ts
    line: 203
    actions:
      - "await fs.rm(dest, { force: true }).catch(() => {});"
    checks:
      - type: input_validation
        code: "if (!location || maxRedirects <= 0)"
      - type: input_validation
        code: "if (total > maxBytes)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: downloadUrlToTempFile
    file: plugins/marketplace.ts
    line: 711
    actions:
      - "await fs.rm(createdTmpDir, { recursive: true, force: true }).catch(() => undefined);"
      - "await fs.rm(tmpDir, { recursive: true, force: true }).catch(() => undefined);"
    checks:
      - type: input_validation
        code: "if (!hasStreamingResponseBody(response))"
      - type: input_validation
        code: "if (Number.isFinite(size) && size > MAX_MARKETPLACE_ARCHIVE_BYTES)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: emitToolResultOutput
    file: agents/pi-embedded-subscribe.handlers.tools.ts
    line: 512
    actions:
      - "const approvalPending = readExecApprovalPendingDetails(result);"
      - "const { buildExecApprovalPendingReplyPayload } = await loadExecApprovalReply();"
      - "buildExecApprovalPendingReplyPayload({"
      - "const approvalUnavailable = readExecApprovalUnavailableDetails(result);"
      - "const { buildExecApprovalUnavailableReplyPayload } = await loadExecApprovalReply();"
      - "buildExecApprovalUnavailableReplyPayload({"
    checks:
      - type: approval_step
        code: "if (!isToolError && approvalPending)"
      - type: approval_step
        code: "if (!isToolError && approvalUnavailable)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: evaluateSystemRunPolicy
    file: node-host/exec-policy.ts
    line: 52
    actions:
      - "const requiresAsk = requiresExecApproval({"
    checks:
      - type: approval_step
        code: "if (requiresAsk && !approvedByAsk)"
      - type: approval_step
        code: "if (params.security === \"allowlist\" && (!analysisOk || !allowlistSatisfied) && !appr)"
      - type: approval_step
        code: if (params.durableApprovalSatisfied)
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: evaluateSystemRunPolicyPhase
    file: node-host/invoke-system-run.ts
    line: 369
    actions:
      - "const configuredAsk = opts.resolveExecAsk(agentExec?.ask ?? cfg.tools?.exec?.ask);"
      - "const approvals = resolveExecApprovals(parsed.agentId, {"
      - "const durableApprovalSatisfied = hasDurableExecApproval({"
    checks:
      - type: approval_step
        code: if (strictInlineEvalRequiresApproval)
      - type: approval_step
        code: "if (policy.shellWrapperBlocked && !policy.approvedByAsk && !durableApprovalSatisfied)"
      - type: approval_step
        code: "if (policy.approvedByAsk && hardenedPaths.cwd && !approvedCwdSnapshot)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: executeNodeHostCommand
    file: agents/bash-tools.exec-host-node.ts
    line: 35
    actions:
      - "requiresExecApproval({"
      - "const requestArgs = execHostShared.buildDefaultExecApprovalRequestArgs({"
      - "await registerExecApprovalRequestForHostOrThrow({"
      - "...buildExecApprovalRequesterContext({"
      - "...buildExecApprovalTurnSourceContext(params),"
      - "} = await execHostShared.createAndRegisterDefaultExecApprovalRequest({"
      - "execHostShared.shouldResolveExecApprovalUnavailableInline({"
      - "execHostShared.createExecApprovalDecisionState({"
      - "execHostShared.buildHeadlessExecApprovalDeniedMessage({"
      - "const followupTarget = execHostShared.buildExecApprovalFollowupTarget({"
      - "void (async () => {"
      - void execHostShared.sendExecApprovalFollowupResult(
      - "} = execHostShared.createExecApprovalDecisionState({"
      - await execHostShared.sendExecApprovalFollowupResult(
      - "await execHostShared.sendExecApprovalFollowupResult(followupTarget, summary);"
      - await execHostShared.sendExecApprovalFollowupResult(
      - "return execHostShared.buildExecApprovalPendingToolResult({"
      - "allowedDecisions: resolveExecApprovalAllowedDecisions({ ask: hostAsk }),"
    checks:
      - type: approval_step
        code: "if (shouldSkipNodeApprovalPrepare({
      hostSecurity,
      hostAsk,
      strictI)"
      - type: approval_step
        code: "if (execHostShared.shouldResolveExecApprovalUnavailableInline({
        trigger: par)"
      - type: approval_step
        code: "if (strictInlineEvalDecision.deniedReason || !strictInlineEvalDecision.approvedByAsk)"
      - type: approval_step
        code: "if (baseDecision.timedOut && askFallback === \"full\" && approvedByAsk)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: executeSystemRunPhase
    file: node-host/invoke-system-run.ts
    line: 526
    actions:
      - "const execArgv = resolveSystemRunExecArgv({"
    checks:
      - type: approval_step
        code: "if (phase.approvedCwdSnapshot &&
    !revalidateApprovedCwdSnapshot({ snapshot: phas)"
      - type: approval_step
        code: "if (expectedMutableFileOperand?.snapshot && !phase.approvalPlan?.mutableFileOperand)"
      - type: approval_step
        code: "if (phase.approvalPlan?.mutableFileOperand &&
    !revalidateApprovedMutableFileOper)"
      - type: approval_step
        code: "if (phase.policy.approvalDecision === \"allow-always\" && phase.inlineEvalHit === null)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: fetchGeminiUsage
    file: infra/provider-usage.fetch.gemini.ts
    line: 14
    actions:
      - "const res = await fetchJson("
    checks:
      - type: input_validation
        code: "if (frac < proMin)"
      - type: input_validation
        code: "if (frac < flashMin)"
    missing:
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: formatExecDeniedUserMessage
    file: agents/exec-approval-result.ts
    line: 72
    actions:
      - "const parsed = parseExecApprovalResultText(resultText);"
    checks:
      - type: approval_step
        code: "if (metadata.includes(\"approval-timeout\"))"
      - type: approval_step
        code: "if (metadata.includes(\"approval-request-failed\"))"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: getPath
    file: media-understanding/attachments.cache.ts
    line: 203
    actions:
      - "await fs.unlink(tmpPath).catch(() => {});"
    checks:
      - type: input_validation
        code: if (params.maxBytes)
      - type: input_validation
        code: "if (size !== undefined && size > params.maxBytes)"
      - type: input_validation
        code: "if (params.maxBytes && entry.buffer && entry.buffer.length > params.maxBytes)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: handleApprovalEvent
    file: acp/translator.ts
    line: 1206
    actions:
      - "const approvalEvent = parseGatewayExecApprovalEventData(params.data);"
    checks:
      - type: approval_step
        code: "if (!approvalEvent)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: handleExecApprovalRequestEvent
    file: acp/translator.ts
    line: 1222
    actions:
      - "const approvalEvent = parseGatewayExecApprovalRequestEventPayload(payload);"
    checks:
      - type: approval_step
        code: "if (!approvalEvent)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: handleGatewayEvent
    file: acp/translator.ts
    line: 652
    actions:
      - this.handleExecApprovalRequestEvent(evt);
    checks:
      - type: approval_step
        code: "if (evt.event === \"exec.approval.requested\")"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: handleInvoke
    file: node-host/invoke.ts
    line: 362
    actions:
      - ensureExecApprovals();
      - "const snapshot = readExecApprovalsSnapshot();"
      - "file: redactExecApprovals(snapshot.file),"
      - ensureExecApprovals();
      - "const snapshot = readExecApprovalsSnapshot();"
      - "requireExecApprovalsBaseHash(params, snapshot);"
      - "const normalized = normalizeExecApprovals(params.file);"
      - "const next = mergeExecApprovalsSocketDefaults({ normalized, current: snapshot.file });"
      - saveExecApprovals(next);
      - "const nextSnapshot = readExecApprovalsSnapshot();"
      - "file: redactExecApprovals(nextSnapshot.file),"
    checks:
      - type: approval_step
        code: "if (command === \"system.execApprovals.get\")"
      - type: approval_step
        code: "if (command === \"system.execApprovals.set\")"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: handlePendingApprovalRequest
    file: gateway/server-methods/approval-shared.ts
    line: 186
    actions:
      - "const hasApprovalClients = params.context.hasExecApprovalClients?.(params.clientConnId) ?? false;"
    checks:
      - type: approval_step
        code: "if (!hasApprovalClients && !hasTurnSourceRoute && !delivered)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: handleToolExecutionEnd
    file: agents/pi-embedded-subscribe.handlers.tools.ts
    line: 880
    actions:
      - "const parsedApprovalResult = parseExecApprovalResultText(rawOutput);"
    checks:
      - type: approval_step
        code: "if (execDetails?.status === \"approval-pending\" ||
      execDetails?.status === \"app)"
      - type: approval_step
        code: "if (parsedApprovalResult.kind === \"denied\")"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: loadAuthProfileStoreForAgent
    file: agents/auth-profiles/store.ts
    line: 355
    actions:
      - fs.unlinkSync(legacyPath);
    checks:
      - type: auth_check
        code: "const authPath = resolveAuthStorePath(agentDir);"
      - type: auth_check
        code: "const statePath = resolveAuthStatePath(agentDir);"
      - type: auth_check
        code: "const authMtimeMs = readAuthStoreMtimeMs(authPath);"
      - type: auth_check
        code: "const stateMtimeMs = readAuthStoreMtimeMs(statePath);"
      - type: auth_check
        code: "const cached = readCachedAuthProfileStore({"
      - type: auth_check
        code: "const asStore = loadPersistedAuthProfileStore(agentDir);"
      - type: auth_check
        code: "writeCachedAuthProfileStore({"
      - type: auth_check
        code: "authMtimeMs: readAuthStoreMtimeMs(authPath),"
      - type: auth_check
        code: "stateMtimeMs: readAuthStoreMtimeMs(statePath),"
      - type: auth_check
        code: "const legacy = loadLegacyAuthProfileStore(agentDir);"
      - type: auth_check
        code: "applyLegacyAuthStore(store, legacy);"
      - type: auth_check
        code: "const mergedOAuth = mergeOAuthFileIntoStore(store);"
      - type: auth_check
        code: "saveAuthProfileStore(store, agentDir);"
      - type: auth_check
        code: "const legacyPath = resolveLegacyAuthStorePath(agentDir);"
      - type: auth_check
        code: "writeCachedAuthProfileStore({"
      - type: auth_check
        code: "authMtimeMs: readAuthStoreMtimeMs(authPath),"
      - type: auth_check
        code: "stateMtimeMs: readAuthStoreMtimeMs(statePath),"
    missing:
      - no confirmation step
    owasp: [ASI-02, ASI-03]

  - function: materializeSubagentAttachments
    file: agents/subagent-attachments.ts
    line: 97
    actions:
      - "await fs.rm(absDir, { recursive: true, force: true });"
    checks:
      - type: input_validation
        code: "if (!limits.enabled)"
      - type: input_validation
        code: "if (requestedAttachments.length > limits.maxFiles)"
      - type: input_validation
        code: "if (estimatedBytes > limits.maxFileBytes)"
      - type: input_validation
        code: "if (bytes > limits.maxFileBytes)"
      - type: input_validation
        code: "if (totalBytes > limits.maxTotalBytes)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: noteSecurityWarnings
    file: commands/doctor-security.ts
    line: 181
    actions:
      - warnings.push(...collectDurableExecApprovalWarnings(cfg));
    checks:
      - type: auth_check
        code: "const resolvedAuth = resolveGatewayAuth({"
      - type: auth_check
        code: "const tokenConflict = resolveGatewayAuthTokenSourceConflict({ cfg, env: process.env });"
      - type: approval_step
        code: "if (cfg.approvals?.exec?.enabled === false)"
      - type: input_validation
        code: "if (dmPolicy !== \"open\" && allowCount === 0)"
    missing: []
    owasp: [ASI-02, ASI-03]

  - function: parseSystemRunPhase
    file: node-host/invoke-system-run.ts
    line: 254
    actions:
      - "approvalDecision: resolveExecApprovalDecision(opts.params.approvalDecision),"
    checks:
      - type: approval_step
        code: "if (opts.params.systemRunPlan !== undefined && !approvalPlan)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: persistBoundedClobberedConfigSnapshot
    file: config/io.clobber-snapshot.ts
    line: 164
    actions:
      - "await params.deps.fs.promises.rmdir(paths.lockPath).catch(() => {});"
    checks:
      - type: input_validation
        code: "if (existing >= CONFIG_CLOBBER_SNAPSHOT_LIMIT)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: persistBoundedClobberedConfigSnapshotSync
    file: config/io.clobber-snapshot.ts
    line: 202
    actions:
      - params.deps.fs.rmdirSync(paths.lockPath);
    checks:
      - type: input_validation
        code: "if (existing >= CONFIG_CLOBBER_SNAPSHOT_LIMIT)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: prepareClaudeCliSkillsPlugin
    file: agents/cli-runner/claude-skills-plugin.ts
    line: 78
    actions:
      - "await fs.rm(tempDir, { recursive: true, force: true });"
      - "await fs.rm(tempDir, { recursive: true, force: true });"
    checks:
      - type: input_validation
        code: "if (linkedSkillCount === 0)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: prepareModeSpecificBundleMcpConfig
    file: agents/cli-runner/bundle-mcp.ts
    line: 76
    actions:
      - "await fs.rm(tempDir, { recursive: true, force: true });"
    checks:
      - type: input_validation
        code: "if (params.mode === \"gemini-system-settings\")"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: processGatewayAllowlist
    file: agents/bash-tools.exec-host-gateway.ts
    line: 265
    actions:
      - "const durableApprovalSatisfied = hasDurableExecApproval({"
      - "requiresExecApproval({"
      - "const requestArgs = buildDefaultExecApprovalRequestArgs({"
      - "await registerExecApprovalRequestForHostOrThrow({"
      - "...buildExecApprovalRequesterContext({"
      - "...buildExecApprovalTurnSourceContext(params),"
      - "} = await createAndRegisterDefaultExecApprovalRequest({"
      - "shouldResolveExecApprovalUnavailableInline({"
      - "const { baseDecision, approvedByAsk, deniedReason } = createExecApprovalDecisionState({"
      - "buildHeadlessExecApprovalDeniedMessage({"
      - "const followupTarget = buildExecApprovalFollowupTarget({"
      - "void (async () => {"
      - void sendExecApprovalFollowupResult(
      - "} = createExecApprovalDecisionState({"
      - await sendExecApprovalFollowupResult(
      - await sendExecApprovalFollowupResult(
      - "const dynamicFollowupText = await resolveGatewayExecApprovalFollowupText({"
      - "const summary = buildGatewayExecApprovalFollowupSummary({"
      - "await sendExecApprovalFollowupResult(followupTarget, summary);"
      - "pendingResult: buildExecApprovalPendingToolResult({"
      - "allowedDecisions: resolveExecApprovalAllowedDecisions({ ask: hostAsk }),"
    checks:
      - type: approval_step
        code: if (requiresHeredocApproval)
      - type: approval_step
        code: if (requiresAllowlistPlanApproval)
      - type: approval_step
        code: "if (shouldResolveExecApprovalUnavailableInline({
        trigger: params.trigger,
  )"
      - type: approval_step
        code: "if (strictInlineEvalDecision.deniedReason || !strictInlineEvalDecision.approvedByAsk)"
      - type: approval_step
        code: "if (!requiresInlineEvalApproval)"
      - type: approval_step
        code: "if (!approvedByAsk &&
        hasGatewayAllowlistMiss({
          hostSecurity,
    )"
      - type: approval_step
        code: "if (hasGatewayAllowlistMiss({
      hostSecurity,
      analysisOk,
      allowlistS)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: readClaudeCliKeychainCredentials
    file: agents/cli-credentials.ts
    line: 410
    actions:
      - "const result = execSyncImpl("
    checks:
      - type: auth_check
        code: "return parseClaudeCliOauthCredential(data?.claudeAiOauth);"
    missing:
      - no input validation
    owasp: [ASI-02, ASI-03]

  - function: registerExecApprovalFollowupRuntimeHandoff
    file: agents/bash-tools.exec-approval-followup-state.ts
    line: 84
    actions:
      - pruneExpiredExecApprovalFollowupRuntimeHandoffs(nowMs);
      - "const idempotencyKey = buildExecApprovalFollowupIdempotencyKey({"
      - "execApprovalFollowupRuntimeHandoffs.set(handoffId, {"
    checks:
      - type: idempotency_key
        code: "execApprovalFollowupRuntimeHandoffs.set(handoffId, {"
      - type: approval_step
        code: "if (!approvalId || !sessionKey || !params.bashElevated)"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: rejectUnsafeControlShellCommand
    file: agents/bash-tools.exec.ts
    line: 1137
    actions:
      - "if (parseExecApprovalShellCommand(candidate)) {"
    checks:
      - type: approval_step
        code: if (parseExecApprovalShellCommand(candidate))
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: releaseAsyncPrefixRoot
    file: test-helpers/temp-dir.ts
    line: 64
    actions:
      - "await fs.rm(state.path, {"
    checks:
      - type: retry_bound
        code: "await fs.rm(state.path, {"
      - type: input_validation
        code: "if (state.activeCount > 0)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: releaseSyncPrefixRoot
    file: test-helpers/temp-dir.ts
    line: 86
    actions:
      - "fsSync.rmSync(state.path, {"
    checks:
      - type: retry_bound
        code: "fsSync.rmSync(state.path, {"
      - type: input_validation
        code: "if (state.activeCount > 0)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: rememberExecApprovalFollowupFailureKey
    file: agents/bash-tools.exec-host-shared.ts
    line: 33
    actions:
      - "if (loggedExecApprovalFollowupFailures.has(key)) {"
      - loggedExecApprovalFollowupFailures.add(key);
      - "const oldestKey = loggedExecApprovalFollowupFailures.values().next().value;"
      - loggedExecApprovalFollowupFailures.delete(oldestKey);
    checks:
      - type: approval_step
        code: if (loggedExecApprovalFollowupFailures.has(key))
      - type: input_validation
        code: "if (loggedExecApprovalFollowupFailures.size > MAX_EXEC_APPROVAL_FOLLOWUP_FAILURE_LOG)"
      - type: approval_step
        code: "if (loggedExecApprovalFollowupFailures.size > MAX_EXEC_APPROVAL_FOLLOWUP_FAILURE_LOG)"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removePathBestEffort
    file: infra/package-update-steps.ts
    line: 58
    actions:
      - await fs
    checks:
      - type: retry_bound
        code: await fs
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: removePathRecursive
    file: infra/update-runner.ts
    line: 254
    actions:
      - await fs
    checks:
      - type: retry_bound
        code: await fs
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: repairPreflightCleanup
    file: infra/update-runner.ts
    line: 260
    actions:
      - "await fs.rm(worktreeDir, { recursive: true, force: true, maxRetries: 3, retryDelay: 200 });"
      - "await fs.rm(preflightRoot, { recursive: true, force: true, maxRetries: 3, retryDelay: 200 });"
    checks:
      - type: retry_bound
        code: "await fs.rm(worktreeDir, { recursive: true, force: true, maxRetries: 3, retryDelay: 200 });"
      - type: retry_bound
        code: "await fs.rm(preflightRoot, { recursive: true, force: true, maxRetries: 3, retryDelay: 200 });"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resetGatewayTestState
    file: gateway/test-helpers.server.ts
    line: 250
    actions:
      - "await fs.rm(stateDir, {"
      - "await fs.rm(tempConfigRoot, {"
      - "await fs.rm(tempConfigRoot, {"
      - "await fs.rm(tempControlUiRoot, {"
    checks:
      - type: retry_bound
        code: "await fs.rm(stateDir, {"
      - type: retry_bound
        code: "await fs.rm(tempConfigRoot, {"
      - type: retry_bound
        code: "await fs.rm(tempConfigRoot, {"
      - type: retry_bound
        code: "await fs.rm(tempControlUiRoot, {"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: resolveToolDeliveryPayload
    file: auto-reply/reply/dispatch-from-config.ts
    line: 1225
    actions:
      - "shouldSuppressLocalExecApprovalPrompt({"
    checks:
      - type: input_validation
        code: "if (shouldSuppressLocalExecApprovalPrompt({
          channel: normalizeMessageChann)"
      - type: approval_step
        code: "if (shouldSuppressLocalExecApprovalPrompt({
          channel: normalizeMessageChann)"
      - type: approval_step
        code: "if (execApproval && typeof execApproval === \"object\" && !Array.isArray(execApproval))"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: rotateConfigBackups
    file: config/backup-rotation.ts
    line: 16
    actions:
      - "await ioFs.unlink(`${backupBase}.${maxIndex}`).catch(() => {"
    checks:
      - type: input_validation
        code: "if (CONFIG_BACKUP_COUNT <= 1)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runAgentTurnWithFallback
    file: auto-reply/reply/agent-runner-execution.ts
    line: 1074
    actions:
      - fs.unlinkSync(transcriptPath);
    checks:
      - type: auth_check
        code: "const authProfile = resolveRunAuthProfile("
      - type: auth_check
        code: "return (async () => {"
      - type: auth_check
        code: "authProfileProvider: runBaseParams.authProfileId?.split(\":\", 1)[0],"
      - type: input_validation
        code: "if (!Array.isArray(value))"
      - type: input_validation
        code: "if (!lifecycleTerminalEmitted)"
      - type: approval_step
        code: "if (evt.stream === \"approval\")"
      - type: input_validation
        code: "if (liveModelSwitchRetries > MAX_LIVE_SWITCH_RETRIES)"
    missing:
      - no confirmation step
    owasp: [ASI-02, ASI-03]

  - function: runCrestodianRescueMessage
    file: crestodian/rescue-message.ts
    line: 140
    actions:
      - "await fs.rm(pendingPath, { force: true });"
      - "await fs.rm(pendingPath, { force: true });"
    checks:
      - type: approval_step
        code: if (APPROVAL_RE.test(rescueMessage))
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runExecResolver
    file: secrets/resolve.ts
    line: 426
    actions:
      - "const child = spawn(params.command, params.args, {"
    checks:
      - type: input_validation
        code: "if (outputBytes > params.maxOutputBytes)"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: runTui
    file: tui/tui.ts
    line: 384
    actions:
      - "const child = spawn(command, args, {"
    checks:
      - type: auth_check
        code: "({ command, args } = resolveLocalAuthCliInvocation());"
      - type: auth_check
        code: "cwd: resolveLocalAuthSpawnCwd({ args, defaultCwd: process.cwd() }),"
      - type: auth_check
        code: "...resolveLocalAuthSpawnOptions({ command }),"
      - type: input_validation
        code: "if (totalSeconds < 60)"
      - type: input_validation
        code: "if (!isTuiTerminalLossError(err))"
    missing: []
    owasp: [ASI-02, ASI-03]

  - function: savePersistedAuthProfileState
    file: agents/auth-profiles/state.ts
    line: 86
    actions:
      - fs.unlinkSync(statePath);
    checks:
      - type: auth_check
        code: "const payload = buildPersistedAuthProfileState(store);"
      - type: auth_check
        code: "const statePath = resolveAuthStatePath(agentDir);"
    missing:
      - no confirmation step
    owasp: [ASI-02, ASI-03]

  - function: scrubLegacyStaticAuthJsonEntriesForDiscovery
    file: agents/pi-auth-discovery-core.ts
    line: 60
    actions:
      - "fs.rmSync(pathname, { force: true });"
    checks:
      - type: input_validation
        code: "if (!isRecord(value))"
      - type: input_validation
        code: "if (value.type !== \"api_key\")"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: sendDirectFollowupFallback
    file: agents/bash-tools.exec-approval-followup.ts
    line: 195
    actions:
      - "const directText = formatDirectExecApprovalFollowupText(params.resultText, {"
    checks:
      - type: idempotency_key
        code: "await sendMessage({"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: sendExecApprovalFollowupResult
    file: agents/bash-tools.exec-host-shared.ts
    line: 407
    actions:
      - ": registerExecApprovalFollowupRuntimeHandoff({"
      - "if (!rememberExecApprovalFollowupFailureKey(key)) {"
    checks:
      - type: approval_step
        code: "if (!rememberExecApprovalFollowupFailureKey(key))"
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: shouldHandleRequest
    file: plugin-sdk/approval-client-helpers.ts
    line: 130
    actions:
      - "!isChannelExecApprovalClientEnabledFromConfig({"
    checks:
      - type: input_validation
        code: "if (params.matchesRequestAccount && !params.matchesRequestAccount(input))"
      - type: input_validation
        code: "if (!isChannelExecApprovalClientEnabledFromConfig({
        enabled: config?.enabled)"
      - type: approval_step
        code: "if (!isChannelExecApprovalClientEnabledFromConfig({
        enabled: config?.enabled)"
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: spawnSubagentDirect
    file: agents/subagent-spawn.ts
    line: 675
    actions:
      - "await fs.rm(attachmentAbsDir, { recursive: true, force: true });"
      - "await fs.rm(attachmentAbsDir, { recursive: true, force: true });"
    checks:
      - type: input_validation
        code: "if (callerDepth >= maxSpawnDepth)"
      - type: input_validation
        code: "if (activeChildren >= maxChildren)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: startChannelApprovalHandlerBootstrap
    file: infra/approval-handler-bootstrap.ts
    line: 42
    actions:
      - "if (isExecApprovalChannelRuntimeTerminalStartError(error)) {"
    checks:
      - type: input_validation
        code: if (isExecApprovalChannelRuntimeTerminalStartError(error))
      - type: approval_step
        code: if (isExecApprovalChannelRuntimeTerminalStartError(error))
      - type: approval_step
        code: if (isRetryableApprovalBootstrapStartError(error))
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: startHandlerForRegisteredContext
    file: infra/approval-handler-bootstrap.ts
    line: 138
    actions:
      - "if (isExecApprovalChannelRuntimeTerminalStartError(error)) {"
    checks:
      - type: input_validation
        code: if (isExecApprovalChannelRuntimeTerminalStartError(error))
      - type: approval_step
        code: if (isExecApprovalChannelRuntimeTerminalStartError(error))
      - type: approval_step
        code: if (isRetryableApprovalBootstrapStartError(error))
    missing:
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: toExecLikeApprovalRequest
    file: infra/exec-approval-session-target.ts
    line: 58
    actions:
      - "if (isExecApprovalRequest(request)) {"
    checks:
      - type: approval_step
        code: if (isExecApprovalRequest(request))
    missing:
      - no input validation
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: updateGooglePromptCacheTtl
    file: agents/pi-embedded-runner/google-prompt-cache.ts
    line: 191
    actions:
      - "const response = await params.fetchImpl("
    checks:
      - type: auth_check
        code: "headers: mergeTransportHeaders(parseGeminiAuth(params.apiKey).headers, params.headers),"
    missing:
      - no input validation
      - no rate limit
    owasp: [ASI-02, ASI-06]

  - function: withTempDir
    file: test-helpers/temp-dir.ts
    line: 105
    actions:
      - "await fs.rm(base, {"
    checks:
      - type: retry_bound
        code: "await fs.rm(base, {"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: withTempDirSync
    file: test-helpers/temp-dir.ts
    line: 166
    actions:
      - "fsSync.rmSync(base, {"
    checks:
      - type: retry_bound
        code: "fsSync.rmSync(base, {"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: withTempHeartbeatSandbox
    file: infra/heartbeat-runner.test-utils.ts
    line: 67
    actions:
      - "await fs.rm(tmpDir, { recursive: true, force: true });"
    checks:
      - type: input_validation
        code: "if (previousValue === undefined)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: withTempHome
    file: plugin-sdk/test-helpers/temp-home.ts
    line: 102
    actions:
      - "await fs.rm(base, {"
      - "await fs.rm(base, {"
    checks:
      - type: retry_bound
        code: "await fs.rm(base, {"
      - type: input_validation
        code: "if (value === undefined)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: writeTarArchiveWithRetry
    file: infra/backup-create.ts
    line: 130
    actions:
      - "await fs.rm(params.tempArchivePath, { force: true });"
    checks:
      - type: input_validation
        code: "if (!isTarEofRaceError(err) || attempt === BACKUP_TAR_MAX_ATTEMPTS)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

  - function: writeUrlToFile
    file: cli/nodes-camera.ts
    line: 77
    actions:
      - "await fs.unlink(filePath).catch(() => {});"
    checks:
      - type: input_validation
        code: "if (typeof contentLength === \"number\" &&
      Number.isFinite(contentLength) &&
   )"
      - type: input_validation
        code: "if (!value || value.byteLength === 0)"
      - type: input_validation
        code: "if (bytes > MAX_CAMERA_URL_DOWNLOAD_BYTES)"
    missing:
      - no confirmation step
      - no auth check
    owasp: [ASI-01, ASI-02, ASI-03]

