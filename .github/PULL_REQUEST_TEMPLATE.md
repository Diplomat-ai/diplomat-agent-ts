
## Summary

<!-- What does this PR change? Why is it needed? -->

## Type of change

- [ ] Bug fix (corrects a false positive or false negative)
- [ ] New pattern (adds detection for a library or API)
- [ ] Feature (new CLI flag, reporter format, or capability)
- [ ] Refactor (no behavior change)
- [ ] Documentation / tests only

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm test` passes (all existing tests green)
- [ ] Fixture added for any new pattern under `tests/fixtures/`
- [ ] `toolcalls.yaml` updated if pattern catalog changed
- [ ] No `--fail-on-unchecked` regression introduced on `./src`
- [ ] Relevant docs updated (README, `docs/`)
- [ ] If user-facing behavior changed, `CHANGELOG.md` updated
