# toolcalls.yaml
# What your agent can do to the real world — and what checks exist.
# Generated by diplomat-agent-ts · https://diplomat.run
#
# Commit this file. Review changes in PRs.
# In source code:
#   Fix          → add validation, the next scan picks it up
#   Acknowledge  → add  # checked:ok  to confirm you've reviewed it
#   Protected elsewhere → add  # checked:ok — protected by [middleware/gateway/etc]
#   CI enforcement → --fail-on-unchecked blocks PRs with new unreviewed tool calls

generated: "2026-05-18T06:06:18.336Z"
version: "0.1.0"
spec_version: "1.0"
path: "./src"
language: typescript

summary:
  total: 0
  no_checks: 0
  partial_checks: 0
  confirmed: 0

tool_calls:
